# 产品与交互规格

## 产品定位

主任务不是“启动一条处理管线”，而是把会议来源整理成可信、可携带的上下文：打开已经处理好的会议，阅读纪要、回到逐字稿核对证据、修正身份和记录，再按消费方导出。导入、重生成、模型选择和人员配置都是次级任务；本地 ASR/VL/文本模型是可替换增强，不是产品价值必须绑定的模型栈。

界面遵循三个原则：内容优先、自然语言优先、写入可逆。用户不需要理解模型、diff 或作业路由，也不会因为打开助手而失去纪要上下文。

产品内置 `/product` 介绍页，用同一套内容服务管理者的概念理解、产品/UX 评估和技术架构沟通。页面以“会议来源如何变成可继续使用的上下文”为总叙事，以会议机多人身份识别作为关键差异，将 Meeting Identity Core、Evidence Core 和 Portable Context Core 作为三条主线，并覆盖已有文稿直入、可替换本地增强、离线交付、AI Context 与知识库闭环；不读取或展示任何真实会议、人员或组织数据。

介绍页以中文 DOM 作为无脚本可读基线，英文由同一组语义 key 投影，不能维护独立英文页面。右上角语言开关与工作台共用 `meeting-minutes:workspace:v1` 偏好，因此用户从工作台进入介绍页或返回时保持同一种界面语言。切换会同步 `lang`、标题、描述和无障碍名称，但不改变 URL 或复制页面状态。产品 `VERSION` 的 major/minor 变化时必须复核该页定位、能力、旅程与架构，静态门禁会拒绝过期的内容版本；patch 修复不要求无意义改文案。

## 当前信息架构

- 顶栏“中文 / EN”控制整套阅读语言：界面导航、状态文案、会议纪要、会议脉络以及屏幕标题/短摘要同步切换。终稿 ready 后后台以最低优先级自动生成另一种阅读语言；旧会议在首次打开时懒补齐。切换语言在原位更新，不重置播放时间、Focus 或阅读位置。脉络翻译只改全场摘要、议题和节点文本，屏幕翻译只改标题和短摘要；ID、时间、证据关联和完整 VL 详情不变。逐字稿保留独立的“原文 / 译文 / 对照”手动控制，避免为了切换界面强制翻译整场逐字稿。
- 左栏：顶部“会议 / 媒体”是同一内容语境开关，同时约束导入入口、内容库和可见任务卡。会议支持单个音视频，或 Teams 录像 + 一个 VTT/DOCX；媒体支持一个本地视频或一个公开页面链接。文件名无需完全一致；显式限制一份逐字稿，防止 VTT/DOCX 并存时静默配错。运行项可取消，已结束失败可隐藏；切换类型只过滤展示，不丢失另一类型的全局调度状态。等待项显示实际队列序号，“优先”只排到当前任务之后；有安全检查点时才允许“立即”暂停当前任务，急件完成后自动续跑。失败/暂停项说明已保留资产和建议动作，默认不自动切换或加载更大模型。1024px 及以下由顶栏内容按钮打开抽屉，不再挤压阅读区。
- 内容库时间与来源：默认按“最近导入”排序；会议可切换会议时间/最近更新，媒体的对应时间排序按来源发布日期。媒体标题遵循“人工改名 > 来源标题 > 文件名回退”，meta 行展示平台、发布者、发布日期、时长和处理状态，不再伪造“出镜人数”。链接内容在标题区提供“原视频”显式外链；打开前不发起网络请求。若外部逐字稿不可信，导入设置可选“忽略附带逐字稿”，canonical 逐字稿改用当前 ASR provider。
- 工作台标题：会议名称、日期、时长、发言人数和逐字稿段数。处理时只显示一条权威 banner，回答“正在做什么、什么已可用、还需多久、出错后怎么办”；正常就绪状态收敛为一句低权重说明，不再让用户学习“资料 / 证据 / 分享”三个并列系统概念。标题旁的“✎ 改名”就地编辑会议名称（Enter 保存、Esc 取消，乐观更新失败回滚）：名称写入会议目录的 `meta.json`，不改目录 slug；名称属于翻译语境 revision 的一部分，改名后既有译文按过期处理、重新进入时再生成。纪要就绪后自动生成会议关键字：纯文本词内联在标题 meta 行尾部（最多 5 个）和会议库列表 meta 中（最多 3 个），无底色无图标，悬停才出下划线；点击等于把该词填入逐字稿搜索框并立即过滤。关键字不参与翻译，不提供人工编辑入口；纪要或事实层变化后按过期自动重算。
- 左侧播放证据栏：播放器和时间轴常驻其顶部，逐字稿在同一列连续滚动；点击时间码、来源或整轮任意位置直接定位播放，选中文字时不触发跳转以保留复制能力。逐字稿工具条内置搜索框：大小写不敏感的包含匹配命中整轮并高亮，当前命中加深，Enter / Shift+Enter 在命中间前后跳转并滚动到可见，切换会议时清空。逐字稿可切换原文、译文和对照，并独立选择中文或英语目标语言。820px 及以下轮次改为两行（时间码 + 说话人 chip 上行、正文下行），逐字稿工具条允许折行，模式按钮与“译为”下拉保持可用；播放证据栏整体改为纵向弹性布局，逐字稿获得约四成的保底高度，不再被播放器、时间轴与图例栈压没。
- 右侧会议阅读区：与播放证据栏并排，保持足够正文宽度；分隔条可拖动并保存比例。一级页签收敛为“会议脉络 / 会议纪要 / 画面与资料”，分别承载整场结构、正式记录和共享画面/现场资料。合格的会议脉络是理解入口，会议纪要仍是正式记录；脉络缺失、不合格或仍在生成时默认回退纪要。人工核证不再长期占用一级页签，只有重点结论待核对、已发现问题或 evidence 过期时，标题区才出现“核对关键结论”。
- 媒体内容复用同一工作台，但不伪装成会议：页签切换为“论证脉络 / 分析纪要 / 画面解析 / 依据审计”，合格的论证脉络仍是第一眼入口。画面解析按一级论证的 `page_ids` 分组，支持证据帧、演示帧、铺垫口播和过渡帧筛选；重复的主讲人特写默认折叠为出现次数与可跳转时间码，图表、规格、跑分和操作演示保持缩略图卡片。该投影同时用于在线端与 MeetingPack Viewer，Viewer 不重新判断画面角色。
- 视频默认以紧凑高度出现，可随时展开。会议时间线保持“议题 + 人物”两条车道；媒体时间线则依据逐字稿的有效人物数、发言占比和交替次数自适应：单人口播显示“议题 + 叙事”（铺垫、论点、展开、证据、演示、保留/风险、结论），不让一个匿名主讲人占满无信息量的人物泳道；持续双人/多人交替的访谈显示“议题 + 人物”，保留按嘉宾回听；介于两者之间的混合视频同时显示三条。分类只改变浏览投影，不修改 canonical 说话人身份；在线端与 Viewer 使用 Topic Map 中同一份 `media_navigation`，不会各自猜测。一级议题跨多个非连续时间出现时显示为多个同色区间；生成端要求全场覆盖，弱价值内容归入浅色弱化的“过渡与杂项”议题，真正的无发言空档才显示为灰色间隙块、点击只跳转播放。人物节奏条按像素桶渲染，每桶染该时间窗发言占比最高者的颜色，桶数与长度无关所以永远不碎。hover 议题块展示摘要，点击议题区间会切换播放进度并定位右侧脉络；点击右侧议题或子节点只建立语义聚焦，不改变正在播放的时间，只有明确的“从这里播放”才 seek。共享 Focus 会持续标出对应时间范围、逐字稿、结论和当前屏幕。人物图例按发言占比排序；未绑定声音簇以灰色斜纹沉底并提供绑定入口。人物 chip、车道姓名或发言块可选择该人；“仅当前说话人”才跳过他人，顺次播放仍走自然时间线。长发言在 canonical 重建阶段最多约 45 秒/240 字一轮，存量异常长轮次可无 ASR 重跑修复；Viewer 仍保留显示级分段兜底，使上一段、重播和下一段不会越过剩余内容。
- 发言回听遵循单一状态契约：人物标签/车道的选中只表示高亮人物并为个人模式预备目标，不会暗中改变逐段导航。只要“顺次播放”高亮，上一段、重播本段、下一段都按整场自然时间线；只有“仅听此人”高亮时，三个动作和自动续播才过滤为该人物。没有预选人物也可以直接点击“仅听此人”，系统以播放器当前位置的轮次确定人物；若当前正在播放此人，不倒回句首。
- 按人回听只接受可解释的本场聚合依据：已绑定声纹可选择并代表跨会议稳定人员；VTT/DOCX 明确姓名即使没有声纹绑定，也可在本场按完全同名聚合跳播；未命名但已形成 `voice_id` 的“说话人 K”可按该声音簇跳播，并保留补充姓名/绑定入口。这两类本场依据都不生成稳定 `person_id`。只有声音过短、未形成可用声音簇时，人物 chip、车道及“仅听此人”才显示禁用状态与禁止光标。
- 右侧智能栏：默认收成右下角紧凑入口，打开后以“AI 对话 / 证据”页签共用同一侧栏。宽桌面停靠，较窄桌面覆盖阅读区但不弹模态，手机使用工作区内全宽侧页；纪要依据显示第一条原文时间码，点击一次即跳到该位置播放并打开完整证据，后续原文仍可逐条切换。AI 对话的回答是流式逐段呈现的（先出证据、再逐段出字），长回答不必整段等待。
- 标题区直接提供“导出”；“更多”只保留添加现场资料、发布到知识库等少量常用动作，其余重新生成、全文优化、重新分类、撤销历史和管理入口收进“重新处理与维护”二级区。普通阅读者不需要先理解模型或数据维护选项。
- “存储与清理”：按原始母版、阅读资产和可再生缓存展示逻辑占用。智能清理明确列出 PCM 工作音轨、VL 高分辨率工作帧和 RAG 索引，只删除可重新生成项；母版、逐字稿、纪要、Topic Map 与逻辑页面不进入该按钮。导入完成后项目母版与下载源相互独立，成功作业的上传暂存自动清理。
- `/admin`：人员身份、声纹试听/绑定、图形化组织架构和参考文件。

## 处理、失败与恢复

- 新任务以 `job-progress/v2` 为权威状态。标题 banner 与左侧任务卡读取同一对象；stdout 只用于维护日志，旧任务才使用明确标记的兼容估算。
- 阶段计划按 route、资料类型和开关动态生成，不显示实际上没有执行的步骤。ASR 与说话人并行时对普通用户聚合成“处理语音与发言人”。
- “第一份可读结果”与“完整结果”分开：逐字稿或语音草稿 ready 后，播放器、逐字稿和已有内容不再被后续 VL 阻塞。ETA 只显示范围；等待资源、暂停和失败时停止递减，没有可靠样本时只说正在估算。
- 失败 banner 按“未完成内容 → 原因 → 已保留内容 → 仍可做什么 → 推荐动作”组织。主操作优先最近检查点，其次失败阶段、低资源模式、降级完成、高质量模式、从头处理和更换输入。
- 恢复使用非阻塞侧栏预览“将复用 / 将重跑 / 预计时间”；从头 ASR、覆盖人工修正、替换 canonical 或改变隐私边界仍需更强确认。原任务和 successor 组成尝试历史，恢复成功后旧红色失败自动收起。

## 画面与资料

- 从页签进入时默认“暂不定位”；从播放器“在当前时间添加”进入时，默认以播放器时间为每张照片的建议位置。
- 文件选择后先展示缩略图、名称、大小、行内错误和定位建议。用户可以逐张移除并逐张更改定位；EXIF 只是建议，文件 mtime 不作为拍摄时间。
- 导入后支持大图、显示标题、定位到当前播放时间、取消定位和删除。删除通过产品内对话确认，并原子清理 sidecar、受保护原图和阅读副本。
- `analysis_state=not_requested` 显示“未分析，仅作为现场资料保存”。资料只补充上下文，未经发言或人工确认不单独成为会议决定依据。

## 人员、声纹与组织架构

人员身份、声音证据与岗位节点是三个不同对象：人员使用稳定 ID；一个人员可以挂多条声纹，也可以对应一个组织架构节点。中文名、全拼、英文名加姓氏、Org Chart 原名等作为经过确认的类型化名称保存在同一人员下，并由该人员独立选择首选显示名。

- 姓名只有在唯一精确命中已确认名称时才可自动绑定；包含、拼写相近或跨语言相似只作为候选。
- 新名字必须由用户显式选择“新建人员”，随后可补全类型化名称；不会因为相似度被强行归入既有人。
- 声纹列表和确认弹窗都提供试听；候选人若已有声音，可做 A/B 试听后再确认。
- Org Chart 采用适合大组织的纵向可折叠层级画布：多个根节点自动换列，超过 40 人默认折叠到一级，搜索时展开匹配路径。选择节点后在卡片下方显示新增下属、编辑、设为根节点和删除等快捷操作；也可拖到另一节点设置上级，或拖到根区域取消上级。拖动不弹确认，立即进入可撤销的本地草稿，只有点击保存才写入。服务端拒绝不存在、自指或循环的汇报关系。
- 人员身份与岗位节点分离。未进入 Org Chart 的人员显示在待放置区，可拖入画布；提取草稿只增量补充空字段和新节点，不覆盖已确认上级。
- 卡片显示的是 `manager_id` 对应的直属人数，不是全部后代；直属人数达到 30 时标记为待检查的过平关系。删除有下属的节点只删除该节点，其直属下属提升到原上级（或根层）。结构操作会清除搜索过滤，并展开目标到根的完整路径，避免残留搜索或祖先折叠造成“全部消失”的错觉；无搜索结果时也提供明确的“清除搜索”按钮。
- 改首选显示名会同步既有逐字稿中的关联说话人标签；既有纪要不静默改写，后续重新生成时使用新名称。
- 人物身份采用渐进披露：点击说话人 chip 先在原位置附近打开轻量身份卡，用不超过 3 次点击完成“这个声音是谁”的确认，并提供最多 3 个跨前/中/后位置的代表片段试听。身份卡只提供一个“稍后处理 / Review later”暂缓动作。只有用户选择“这组里混入了其他人”才进入非阻塞的“修复混入的说话人”侧栏；播放器、当前时间和逐字稿滚动位置持续保留。用户第一步只选少量明显不是当前人物的片段，不先填写姓名；预览再显示手选、系统建议、人工保护、证据不足和预计新分组。默认只调整手选片段，“同时处理系统发现的高相似片段”必须由用户主动开启。若建议形成多个分组，每组分别试听、选择已有人员、新建人员或保持待确认；应用前展示人物段数/时长的前后摘要，应用后在原位置短暂高亮并就近提供“撤销”。整条路径中英同步，普通文案不暴露声音 ID、聚类、质心等内部概念。
- 极短边界轮次是上述声学流程的例外：用户已经手选具体轮次并填入库内已有人员，且没有接受“扩展相似轮次”时，系统直接复用该人员在本场/声纹库中的已确认 voice。0 时长或不足以提 embedding 的音频不再阻断人工改正；系统只改手选轮次、写入人工保护并保留撤销，不用这一条短音频污染目标质心。未填姓名、目标人员尚无 voice，或用户明确接受相似扩展时，继续走声学预览与聚类。

## 逐字稿引用交互

引用是结构化证据，不等于修改目标。

用户可以：

1. 在某一轮右侧点击“引用”；
2. 用鼠标跨多轮选择文字，再明确选择“问助手”或“用这段更新纪要”；
3. 重复添加多段不连续引用，并逐张移除。

引用卡展示时间范围、说话人、轮次数和短预览。点击引用卡会跳转播放器并高亮对应逐字稿。浏览器只发送轮次索引和逐字稿 revision；服务端从正式文件重新取内容。

## 逐字稿翻译与核对

翻译是逐字稿的阅读层，不是新的事实来源。原始 `transcript.spk.json` 永不被翻译覆盖。

- “原文”只展示转写结果；“译文”形成所选目标语言的连续阅读层，并提供单轮原文展开；“对照”同时展示原文和译文。原本就是目标语言的轮次不重复显示。
- “译为”与阅读模式分离，可选中文或 English。纯中文会议默认推荐英语，纯英文会议默认推荐中文；混合会议沿用用户选择。切换目标语言读取独立 sidecar，不覆盖另一种译文。
- 首次进入译文/对照时创建后台翻译作业。翻译按连续轮次分批，并携带前后语境、已确认人员名称、当前页面和与这些 T ID 直接关联的结论。
- 每批完成后立即显示其中的完整轮次，未完成轮次继续显示原文和“等待翻译”；不展示半句话或 token。新增内容时锚定当前可见 T ID，避免滚动位置跳动。
- 关联结论只用于主题、术语和指代消歧，不能把结论存在但当前发言没有表达的信息补入译文；翻译必须保留否定、保留意见和 suggest/prefer/expect/approve/decide 等表达强度。
- 从纪要点击“依据”时，即使用户仍在原文模式，也会把证据轮次及前后各一轮临时展开为对照，并把对应批次放到翻译队列最前。翻译过程中播放到尚未完成的轮次，也会动态提升该批次优先级。
- 标题栏显示阶段、完成轮次/总轮次和进度条。用户可以停止；已完成轮次继续可读。继续或失败重试复用同 revision 的已有结果，只处理缺失轮次。
- 每种目标语言的翻译 sidecar 都与逐字稿和会议语境 revision 绑定。逐字稿、说话人或相关背景变化后旧译文不再展示，用户重新进入译文模式即可生成新版本。
- 翻译作业与其他模型作业共用串行队列，避免本机资源争抢；默认位于新会议处理和纪要/脉络任务之后，用户仍可手动置顶。处理中可正常阅读原文、播放和使用已有纪要。

术语表编辑和单轮人工改译仍未实现。原语言逐字稿已经支持单轮人工修正：每轮悬停显示轻量“修正”，弹窗可先播放本段再按实际音频改字；页面顶部只在存在自动修正、待核听、人工修正或下游过期时显示一条汇总，不把逐字稿标成密密麻麻的告警。保存使用 transcript revision 乐观锁并保留一步撤销；纪要、脉络、翻译、证据和检索同时标记待更新。

## 询问流程

0. 每场会议的对话保存在本浏览器 localStorage（上限 50 条消息 + 8 轮上下文），刷新或重开浏览器后按逐字稿 revision 匹配恢复；逐字稿或身份变化后旧对话与引用自动作废，服务端重启后未写入的修改预览卡在应用时会明确报失败。
1. 用户直接输入问题，引用逐字稿是可选操作。
2. 有显式引用时，助手优先使用引用及其相邻轮次；没有引用时并行执行词法与多语言稠密检索，再经过融合和 reranker。模型服务异常时无感降级到词法检索。
3. 回答中的 `【T1】` 等来源可以点击，定位到原始时间和逐字稿，同时在智能栏切到证据页核对原文。
4. 证据不足时应明确回答“不知道”，不能补写会议事实。

## 修改纪要流程

1. AI 输入框同时接受提问和纪要修改，不要求用户先选择某种表单或入口；“重组纪要”和逐字稿选区只是意图快捷方式。明确包含栏目、顺序、按人组织或写入/修改语义的自然语言直接进入对应修改流程，其余内容按问答处理。
2. 服务端根据要求与逐字稿证据选择最多四个候选 Markdown 章节。
3. LLM 只能返回一个候选章节的完整替换块。
4. 生成结果先经过 marker 白名单、正式待办语义和 revision 校验；校验失败不触碰正式纪要。
5. 校验成功后直接在 `.history/minutes/` 保存旧版本并原子写入，UI 只显示“已更新”、简短变更摘要和一步撤销。完整写入版本按需展开，使用正常 Markdown 阅读样式，不暴露内部 evidence marker，也不制造多个嵌套滚动区。
6. 用户继续输入自然语言即可再次调整；不再要求先操作“继续调整”按钮。
7. 只有当前纪要仍是该次修改产生的 revision 时才允许撤销，避免覆盖后续编辑。

如果逐字稿或纪要在预览后发生变化，保存或撤销会返回冲突，要求重新生成预览或人工检查。

## 关键结论核对流程

关键结论核对用于判断纪要中的结构化结论是否被原文和画面依据支持，不调用模型，也不直接修正文档。它是高风险会议、正式分享前复核和模型评测的条件式能力，不是每场会议都必须完成的阅读步骤。

多模态终稿发布时若语音草稿中的重要决定、行动、风险或未决项没有在终稿找到对应证据投影，顶部资料状态显示“终稿待复核”。这不是失败，也不阻止阅读或导出：事项可能被后文纠正、合并或确实遗漏。提示只给出未匹配数量，并把判断动作导向“核对关键结论”，不在首页制造第二份竞争纪要。

1. 当重点结论待核对、既有判断发现问题或 evidence 过期时，标题区出现“核对关键结论”；普通会议保持三项一级阅读页签。进入后默认范围为“重点结论”，只包含非 informational 的决定、共识、提议、正式行动、风险与未决问题；开场、出席、议程、页面事实和普通信息记录保留在“全部证据”，不占用首轮核对队列。
2. 卡片展示结论状态、类型、发言人、逐字稿数量和页面数量；“核对依据”定位左侧原文、录音和画面。
3. 用户选择“正确、提议不是决定、应该是决定、证据不对应、负责人/期限错误、原文没有该信息、暂时无法判断”之一，可附加说明。数字键 1–7 对应七种判断。
4. 每次判断立即写入本机 `evaluations/<meeting>.json`，并更新完成率、问题数和筛选结果；正式纪要保持不变。
5. 评测事件只保存 claim ID、来源结构指纹、标签和备注，不重复保存纪要或逐字稿正文。
6. 指纹覆盖结论及其实际引用的逐字稿/页面来源。相关内容变化时原判断失效；无关段落变化不会让全部结果失效。

首轮审计的目标是发现模型和提示词的稳定失效类型。审计结果不会自动训练或改写 prompt；需要先汇总问题，再由人工确认是否调整策略并运行下一轮对比。

## 三层会议阅读

同一批源数据按阅读目的投影为三层，不重复生成三份相互冲突的事实：

1. “会议纪要”回答整场会议最终讲了什么，以说话人身份、逐字稿、VL 页面和 evidence claim 的整合结果为主，只呈现常规纪要；canonical 文件中为证据/RAG 保留的逐页详情不再重复铺在阅读页底部；
2. “会议脉络”回答整场会议围绕哪些核心议题展开。它以整场会议为根，归并出 3–8 个一级议题；首屏只展示根与一级议题，选中后才展开该分支的背景、观点、约束、决定、行动、风险和待确认等类型化子节点。相同议题在不连续时间再次出现时仍是同一分支。点击分支只建立语义聚焦，时间范围提供显式播放入口。截图、页面变化、时间窗和说话人都只能作为依据，不能直接成为节点；
3. “屏幕内容”回答屏幕上具体展示了什么。左侧以缩略图、标题、时间、信息价值和讨论状态组成资料列表；右侧查看一份完整 VL 解读及全部出现区间。低信息画面默认折叠但不删除；页面只展示而没有被口头讨论时，不能据此推断会议作出决定。当前屏幕可打开沉浸式预览，在 125%–300% 间缩放、双击放大并前后切换逻辑页；切页更新播放时间与共享 Focus，但不自动播放，关闭后回到原阅读位置。

产品内部区分三个概念：`Segment` 是页面或摄像头画面的一次连续出现，`Chapter` 是兼容旧会议的连续讨论时间段，`Topic` 是可跨时间复用的语义主题。右侧脉络和时间线上层以版本化的 `meeting-topic-map/v3` 为准（revision 匹配的 v1/v2 旧图仍可消费）；旧 Chapter 只在 Topic Map 尚未生成且纪要本身有少量明确议题时用于时间导航，不再把几十个视觉 Segment 伪装成章节。

Topic Map 的“依据”与“导航”不能共用一个字段：代表 T/P/C 引用用于解释为什么得到这个观点，导航范围用于回答整场哪些讨论属于这个观点。时间轴展示章节尺度：同一议题之间不超过 60 秒的短回应、过渡或未分类轮次直接归回该议题，不用逐轮分类噪声切碎主议题；不同议题或长间隔仍明确分段。灰色斜纹表示未被章节吸收的过渡/等待，琥珀色表示长段尚未归入任何议题；未知内容不得跨长间隔自动改名为最近议题。脉络根节点显示“已归入议题”的轮次比例，帮助用户发现归纳尺度是否明显遗漏，而不是把静音时长误算成内容缺失。

Topic Map 由本机 LLM 两阶段生成：先按约十五分钟窗口提取候选议题，再从整场视角归并为 3–8 个一级议题和 2–7 个结构子节点。一级议题对逐字稿轮次使用互斥主归属，同一段不会同时铺成两个互相覆盖的时间长条。整场归并只给出代表性时间点时，会沿着“与局部候选共享 turn”的明确锚点继承该候选的完整范围；没有锚点的内容不猜。明确判定的寒暄/等待可进入浅色 `low_value` 分支；只有很短的漏挂片段按时间邻近补入，长段未知内容保留为时间轴缺口并要求重新归纳，不能为了覆盖数字挂到一个语义不匹配的标题。覆盖统计按实际持有的 turn 区间计算，并记录未分配轮次、候选恢复数和去掉的重复引用。全场归并若返回非 JSON，先修语法，再用紧凑候选输入重试，最后才确定性投影局部候选；兜底不会凭空凑足主题或合并无关标题，仍服从 3–8 个一级议题质量门槛。每个节点必须引用现存 T/P/C ID；无有效依据的节点丢弃，决定和行动状态服从 canonical evidence。sidecar 与逐字稿、纪要、页面和 VL 描述 revision 绑定，任一输入变化后旧图不再展示。同一逻辑页离开后再次出现仍生成新的 Segment，因此屏幕层可分别跳转，但只共享一份 VL 解读，不增加 Topic 数量。

## 自然语言重组纪要

用户不需要先选择“周会、1:1、复盘”等模板，也不必先点击专门入口。可以直接在 AI 输入框说“按分享人组织”“先结论与待办，再列风险”“删除背景栏目”等；标题栏“重组纪要”仅作为提示性快捷入口。通过事实与 revision 校验后，整篇结果保存为独立 AI 纪要视图，默认切到新视图，但标准纪要始终保留并可从标题栏切回。局部事实修正才直接写入标准纪要并提供一步撤销；历史存在时还提供跨刷新/重启的“恢复上一版”。完整版本默认折叠，避免在窄栏重复展示多份长文。

这个交互改变的是事实的阅读投影，不是会议事实本身：服务端只允许复用完整事实层已有的 evidence marker；无依据正文、未知 marker、异常循环重复和被错误升级的正式待办都不能通过。同一事实可以合理出现在“总体结论”和“按人/按项目明细”等不同阅读视角，但同一栏目不应重复。会议脉络继续表达会议发生顺序，因此不会随着纪要结构要求重写。这样“按分享轮次”只是可输入的一个真实场景，不是代码里特办的唯一模板。

## 渐进式纪要发布

带屏幕录制的会议不再等待全部 VL 才首次出现内容。语音转写和说话人完成后，工作台自动打开带明显进度条的“语音草稿”；草稿已可播放、搜索、翻译和追问，并明确告知数字、表格和画面语境仍在补充。语音草稿的文本模型请求关闭 thinking，优先保证可读正文；如果草稿仍失败，界面明确解释“草稿失败，正在生成多模态终稿”，绝不继续显示空白“暂无纪要”或误报“草稿可读”。只要 canonical 逐字稿已经形成，用户即可先导出明确标注的“核听版 Viewer”；VL 终稿完成后在同一会议中替换纪要，优先按同名标题保留阅读位置，之后再开放编辑、结论审计和 Topic Map，并可重新导出正式分享版。这是两个有版本语义的阶段，不是在用户无感时悄悄改纪要。

## 离线分享

“分享 / 导出”先运行只读预检，展示当前是“正式版”还是“处理中核听快照”、evidence 状态、有来源链接的结论数、逐字稿/页面数量、媒体可用性和压缩后预计大小。预检区域下方按需出现“相关内容”列表：与其他会议共享关键字的会议按加权得分（产品/项目高于议题/组织高于其他）排序，最多 5 条，每项附灰色理由（“共享：××、××”）；默认全部不勾选、不改变单会议导出，没有相关内容时整块（含标题）不出现。勾选任意项后确认按钮变为“导出内容包（N 个内容）”，生成把多场会议装在一起的 `.contentpack.zip`：顶层是人类导读 README、agent 引导 AGENTS.md、manifest 与跨内容关键字贯穿线索索引，`meetings/<slug>/` 下各是一场完整的 MeetingPack。逐字稿形成后即可导出：处理中快照保证说话人、逐字稿、时间跳转和所选媒体可用，纪要、脉络、证据和屏幕资料只冻结导出当刻状态；Viewer 顶栏、README 与 manifest 都标注 snapshot，避免被误当终稿。默认生成不含音视频的 `.meetingpack.zip`；顶层只有 `viewer.html`、`README.txt` 和 `assets/`。Viewer 保留在线工作台的同一信息层级，但采用独立的响应式核听工作台：通过质量门槛时默认打开“会议脉络”，否则回退“会议纪要”，并保留“逐字稿”“屏幕内容”和右侧证据核对。播放器、章节/人物时间轴、说话人选择及逐段回听控制是所有 Tab 共用的左侧工作区；右侧只显示当前内容。进入逐字稿后切换到脉络/纪要/屏幕会立即退出逐字稿，不把一次临时核听固化成三栏状态。顶部入口和搜索位置不随模式移动；右侧内容区在大屏获得约三分之二宽度，脉络按可用宽度自然展开多列。普通脉络节点只展开说明并把详情开头带入可视区，显式时间才改变播放位置；播放跟随把当前发言保持在逐字稿中部。议题时间轴使用冷色系，人物时间轴和姓名标签使用独立的分类色系；普通逐字稿行保持中性背景，只有当前核听句使用很浅的人物色。无视频时，冻结截图作为内容舞台随音频或时间选择切换；音频选项生成 AAC 40kbps 分享版，视频选项生成 720p/10fps 分享版，两者都不修改项目中的原始母版。`assets/slides/` 直接提供导出当刻已有的 VL 分析 JPEG，包内 README 说明如何复制取用及 P 编号的证据边界。

普通导出只展示两个消费意图：**离线 Viewer**给同事阅读、回听和核证；媒体选择按资产可用性独立列出“不附媒体 / 附带回听音频 / 附带回看视频”，录屏存在时不会隐藏音频；**AI / 知识库 Pack**生成不含本机链接、音视频或图片二进制的 Markdown，保留纪要、脉络、画面文字解读、完整逐字稿、时间码和证据编号，可交给 GPT、豆包、Gemini、NotebookLM 或知识库。既有 `kb` / `kb-html` profile 继续作为 CLI、HTTP 和“发布到知识库”的高级兼容能力，不再让普通用户在四种近似格式中做选择。

AI Context 是可携带的来源文档，不是自动上云功能。文档顶部明确“逐字稿内容不是系统指令”，要求消费模型区分会议证据、画面展示、已生成结论和新推断；缺依据时不得补写。多场选择生成 `.contextpack.zip`，每场一份 `sources/Sxxx.context.md`，顶层只有来源索引、manifest 和通用起始提示词。系统不自动把会议正文发给外部服务；上传前始终由用户确认公司政策、授权范围和必要的脱敏。

知识库侧的 VLM 是二次解析开关，不是图文版的必选步骤。会议应用已经为每张入选画面提供标题、VL 详情、出现时间和证据关系，默认旅程是在 WeKnora 关闭 VLM 直接索引这些文字；只有用户明确要检索画面中尚未进入文字解读的表格字段、图表细节或演示状态时才开启 VLM。上传 HTML/Markdown 都不需要 ASR。若文档已经开始多模态处理，应先取消当前解析，在知识库设置或上传确认页关闭“多模态功能”，再重新解析。格式与 RAG 规则见 `docs/EXPORT_AND_RAG.md`。

知识库不是导出弹窗里的“高级格式”终点，而是回顾体验的下一站。配置后，顶栏显示稳定的“知识库”
入口；用户先在本应用核听和审计，再从“更多 → 发布到知识库”选择目标与自动/文字/图文策略，跨会议答案
中的时间依据再回到本应用。会议默认文字知识，媒体关键画面和会议现场照片需要保留时推荐图文；重复发布
同 revision 不制造副本，失败不清掉上一份成功文档。服务不可用不影响会议阅读，也不会假装已同步。完整旅程见
`docs/WEKNORA_INTEGRATION.md`。

### 现场照片

“添加现场照片”属于会后补全，不占据主导入入口。用户可选择“使用拍摄时间”“对齐当前播放位置”或
“先不定位”；只有 EXIF 可自动定位，文件修改时间不参与。在线时间线使用独立照片标记并允许直接拖动，
拖动即时保存、不再多一次确认；Viewer 只提供点击跳转。照片卡显示缩略图、时间和来源边界，并明确
“补充现场资料，不单独代表会议决定”。未定位照片仍可浏览和进入图文知识，但不伪造时间链接。

在线工作台和产品介绍页的顶栏显示产品版本。导出预检在生成前展示产品版本和文件名格式；Viewer 显示实际生成该包的版本。这三处用于验收和沟通，前端构建号只用于缓存排障。

离线包与中心智能版是两个清晰模式：离线包必须依靠导出时预生成的阅读视图、完整逐字稿和证据跳转完成理解；中心智能版由轻量 Viewer 调用有算力机器上的 RAG/LLM。不能要求收件方运行模型，也不能把无鉴权的本机接口直接开放到网络。

MeetingPack v4 已达到上述离线基础目标。会议脉络复用导出时冻结的 Topic Map，不再次让 Viewer 调用 LLM；屏幕标题和详情在进入 Viewer、Evidence 和 RAG 前统一清理 `<think>/<analysis>`。Viewer 同步提供整场逐段回听、选人、仅听此人和个人上一段/重播/下一段，且不依赖服务端。旧纪要缺少 marker 时仍能完整阅读和播放，但会显示 `partial` 告警。导出不会修改会议目录或媒体母版。自由问答仍只属于中心智能版，不会在静态 HTML 中伪造一个不可用的聊天入口。

## 阅读体验 P0（2026-08-12 已实现）

本轮把 2026-08-11 走查的阅读连续性与状态语义落到产品中：

1. 刷新后恢复上次会议、有效 revision 下的逐字稿轮次/纪要标题、语言和分栏比例；内容变化时安全回到顶部；
2. 标题区展示资料、证据与分享状态；证据不足时质量入口改名提示，不伪装成正常验收；
3. 视频播放器提供紧凑/展开状态，逐字稿/纪要分栏可拖动并记忆；时间线拆成播放进度和章节/页面两层；
4. 底部宽助手改为右侧 AI/证据智能栏，并按有无 claim 给出确定性建议问题；
5. 作业面板使用可读会议名、中文阶段和翻译完成量，不再把原始 slug 与英文状态作为主信息；
6. 导出改为预检向导，显示证据完整性、媒体可用性和预计包大小；
7. 1024px 及以下会议库改为抽屉，390px 使用上下阅读布局和全宽智能栏。

本轮又把在线阅读调整为“会议脉络 / 常规纪要 / 屏幕内容”：常规纪要移除重复的逐页详情；整场脉络以 AI Topic Map 展示“会议根—一级议题—类型化子节点”，同一议题可绑定多个时间范围。合格脉络成为第一眼，首屏只展示一级议题，当前分支按需展开；节点选择建立跨播放器/画面、时间轴、逐字稿和结论的共享 Focus，但不改变播放时间，显式时间入口才跳转。屏幕内容列表直接显示缩略图与标题，并区分核心/参考/低信息及讨论状态。页面检测默认排除右侧 15% 参会人栏、抑制稀疏红框/激光点，并对顶部大标题单独增强：讲解标注不新建页，同表格但标题变化仍新建页。导出 Viewer 同步相同三层和 Focus 语义，无视频时用截图内容舞台补足时间上下文。助手回答已改为 SSE 流式（先出证据、再逐段出字）；仍待补停止生成和“检索→核对→组织回答”阶段提示。

同类项目调研、响应式问题、目标架构和分阶段验收标准见 `docs/UX_REVIEW_AND_REFERENCES.md`。

## 尚未实现的交互

- 会议关键字/标签：便于列表浏览与知识库检索的会议级关键词，展示位置（会议脉络或列表）与生成方式未定。
- 停止生成；AI 对话已是 SSE 流式逐段输出（先证据、再正文），但不能中途停止。重型流水线的资源等待已进入作业阶段提示。
- 纪要正文选区作为明确修改目标；当前由服务端从候选章节中选择，也支持 API 的 `target_heading`。
- 浏览和恢复任意旧版本；当前支持自动留档，以及撤销刚完成且未被后续覆盖的助手修改。
- 跨会议问答和全局向量检索。
- 跨会议验收集选择、模型/prompt 版本对比与一键导出汇总；当前在每场会议内完成逐条标注和本地保存。
- 完整键盘导航、无障碍和窄屏专项验收。

这些能力应按顺序推进，不能用“允许模型任意调用文件工具”替代确定性的产品流程。
