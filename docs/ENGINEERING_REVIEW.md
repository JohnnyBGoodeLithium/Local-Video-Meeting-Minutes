# 工程走查记录

- 首次走查：2026-08-10
- 最近更新：2026-08-12
- 状态：持续维护

## 维护约定

本文是工程评审台账，不是一次性报告。出现以下情况时必须同步更新：

- 完成影响产品交互、数据格式、生成策略、隐私边界或运行架构的改动；
- 修复真实环境事故，或发现合成测试未覆盖的失效路径；
- 某一“仍需推进”事项完成、部分完成、取消或优先级改变；
- 开始新的大型能力之前，对依赖项、数据安全和回归范围进行复核。

更新时记录结果、对应 Git commit、验证方式和仍然存在的限制；完成项移入“已处理”，只有基础设施已具备但用户闭环尚未完成的事项标为“部分完成”。评审文档不得写入真实逐字稿、纪要正文、人员名单、凭据或私有原始路径。

## 已处理

- 建立 Git 基线，并通过 `.gitignore` 隔离全部真实会议、录音、声纹、组织架构和运行状态。
- 分离代码根与数据根，测试/部署可独立指定私有数据位置。
- 将固定共享 `_smoke` 夹具改为每次独立临时根，消除多会话相互删除夹具的问题。
- 上传验证失败时清理半上传目录，并拒绝多音频、混合音视频和不明确的多 VTT 请求。
- 作业 JSON 不再保存任意 stderr，只保存受控元数据；dry-run 结果进入明确的 `result` 字段。
- 增加健康检查、环境 doctor、统一 Makefile 命令和依赖声明。
- 增加本地会议助手：结构化逐字稿引用、来源跳转、问答、纪要修改预览、revision 冲突保护、确认写入与历史备份。
- 将三栏工程控制台重构为会议回顾工作台：左侧播放/时间轴/逐字稿证据栏常驻，右侧阅读纪要，导入/作业/模型入口降级，底部自然语言助手常驻。
- 纪要修改改为可读的“保存/继续调整/取消”确认卡，并增加 revision 保护的一步撤销；自动回归覆盖应用与撤销闭环。
- 声纹库升级到 v3：稳定人员 ID、国际化类型名称和每人独立首选显示名；近似姓名只给候选，不再自动绑定。
- `/admin` 增加后台声音试听与候选 A/B 试听，并将 Org Chart 改为可增删、拖动改上级、服务端校验环路的图形画布。
- Org Chart 提取不再自动翻译、生成拼音、合并跨语言变体或创建虚假上级；草稿以增量方式进入人工确认流程。
- Org Chart 提取会在解析前剥离模型 thinking/analysis，并拒绝提示词回声行；逐页原始输出保存在私有数据根，便于本机排错但不进入作业日志。
- 回归覆盖近期遗漏的姓名新建、作业取消、上传清理、助手问答/修改、删除与数据根隔离。
- 纪要生成改为结构化 `meeting-minutes-prompt/v1`，用稳定 T/P/C ID 关联结论、逐字稿和页面；职级只作为决策权限语境，不直接决定结论权重。全部 VL 页面进入“有讨论/仅展示”附录（commit `829fa8d`）。
- 增加 `minutes.evidence.json`、版本化 RAG JSONL 和无需服务/LLM 的 `.meetingpack.zip` 静态查看器；Web 以轻量“依据”链接展示来源（commit `829fa8d`）。
- 修复旧录音缺少会议内 `audio.wav` 和视频 inbox 源路径消失导致的播放失败；新导入媒体固化到会议目录，同盘优先硬链接、跨盘复制，旧会议保留 `source.json` 回退（commit `7a9829f`）。
- 增加会议内“质量验收”工作区：标题栏提供显式入口，按 evidence claim 逐条标记正确或错误类型，支持原文/播放依据跳转、待判断/问题/正确筛选和 1–7 快捷键；本地追加式评测事件不改正式纪要、不复制 claim 正文，相关来源变化后判断自动失效。首页壳禁止缓存，避免服务升级后浏览器长期停留在旧入口（commits `e15f3ba`, `bfcd603`, `e46c185`；显式入口补丁回归 80/80）。
- 增加上下文感知的逐字稿中文阅读：原文/中文/双语切换、单轮原文展开、纪要依据跳转时自动双语展开相邻上下文、后台串行翻译和独立 revision sidecar。完整轮次按批次渐进显示，带进度条、停止/继续、失败续跑、滚动锚定，以及依据/当前播放轮次动态优先；批次顺序变化不改变原始语境。翻译使用页面、人员与关联结论消歧，但结论明确为低信任背景，不能向原话补事实（commits `2b46c70`, `8e829ec`；合成 Web 回归 85/85，另有优先/部分取消/续跑单元回归）。
- 会议助手接入 `meeting-rag/evidence-hybrid-v1`：统一检索当前会议的 claim、逐字稿、VL 页面和纪要章节，命中二次归纳时补回原始逐字稿，统一 `R` 引用可点击回到播放位置；增加不调用模型的检索检查接口。旧会议没有 claim linkage 时显式标记 `partial`，不把纪要章节伪装成证据（合成 Web 回归 86/86，纯虚构内容的本机真实 LLM 协议测试通过）。
- MeetingPack 升级为 `meetingpack/v2`：离线 Viewer 常驻完整逐字稿、播放器和时间轴，任意时间码可跳转；增加管理层/执行层 × 快速/精细四种确定性视图、会议理解图、右侧证据核对和跨结论/原文/页面搜索。旧会议证据不足时显式标记 `partial`，导出始终保留完整逐字稿且不反写会议 sidecar（合成 Web 回归 88/88，实际音频包完成浏览器视觉检查）。
- 会议 RAG 完成多语言 embedding + reranker：Qwen3 0.6B 双模型以独立 loopback user service 常驻，词法/稠密两路经 RRF 融合后重排，失败自动降级；向量索引按会议 revision 持久化且 manifest 不复制正文。6 场现有会议共 2545 条记录预热成功，中英双向合成检索和本地助手协议测试通过。
- MiniMax H3/ComfyUI 试跑导致本机内存压力后，停止 H3 图像服务并恢复默认文本模型组合；检索小模型增加单并发、4K context、关闭额外 cache 及 systemd 3G/5G cgroup 限制。预热后两服务约占 1.6G + 1.0G，未再次出现失控占用。
- 完成部署界面 UX 走查与 GitHub 同类项目评估：确认当前证据核对、可逆编辑和离线 Viewer 是差异化主线，同时识别刷新不恢复会议、状态语义不清、固定媒体/分栏压缩阅读区、作业阶段缺失和知识停留在单会议等问题。目标工程路线确定为“抽共享 domain core → 版本化 FastAPI routers → 渐进迁移 Vue 3/TypeScript”，暂不整体重写为 Tauri；同类功能与许可证边界记录在 `docs/UX_REVIEW_AND_REFERENCES.md`。
- 完成阅读体验 P0：启动自动恢复上次/最近会议，按 transcript/minutes revision 保存轮次与标题锚点、语言和分栏比例；视频默认紧凑，时间线区分播放进度与页面/章节，分栏支持鼠标/键盘调整。标题区增加资料/证据/分享状态；1024px 及以下会议库进入抽屉，手机使用上下阅读布局（本次提交）。
- 将底部全宽助手重构为右侧“AI 对话 / 证据”共用智能栏：宽桌面停靠、窄桌面覆盖、手机全宽；逐字稿引用与来源点击自动切换上下文，并按 evidence 是否存在提供确定性建议问题（本次提交）。
- 导出入口合并为只读预检向导，服务端只返回 evidence linkage 数量、内容数量、媒体可用性与预计体积，不泄露正文/路径；作业日志读取改为逐行受控元数据并投影中文阶段，翻译继续显示完成量（本次提交；隔离 Web 回归 89/89）。
- 增加内置 `/product` 产品介绍页，并从会议工作台和人员设置页双向进入。叙事首先解释会议机共用账号导致的人员名称不可区分，再把声纹试听、稳定人员 ID、国际化姓名与可编辑 Org Chart 组合为 Meeting Identity Core；后续分层介绍 Evidence/Knowledge Core、用户旅程、UX、技术架构、可信边界和路线图。页面零外链、零真实数据，支持桌面与手机响应式展示（本次提交）。
- 修复纪要待办和多语言逐字稿的阅读可靠性：新 Prompt 使用独立待办标题与固定表头，渲染端确定性修复旧纪要中紧贴列表项/全角竖线的表格，`minutes.evidence.json` 增加带 claim/T/P linkage 的结构化 `actions`；逐字稿改为“原文 / 译文 / 对照 + 中文/English 目标语言”，中英译文使用独立 sidecar；说话人姓名仅做定宽省略，完整国际化姓名不被截断。隔离 Web 回归 92/92，另有旧表格/行动项和中英翻译单元回归（本次提交）。
- 在线三层阅读使用“会议纪要 / 章节脉络 / 屏幕内容 / 结论审计”：双层时间线上层表示可跨时间复用的 Topic，下层表示页面每次实际出现的 Segment；同一逻辑页多次返回会生成独立 Segment，但复用一份 VL 解读。新增 `meeting-topic-map/v1` 两阶段语义归纳，把整场会议组织为 3–8 个一级论点和类型化子节点，并强制绑定 T/P/C evidence；不再把页面或几十个视觉片段当脉络节点。时间轴点击 seek + 定位，右侧节点点击只查看，范围按钮才播放。页面变化检测默认排除右侧 15% 参会人栏；真实会议只读对比将 57 个逻辑页面中的 8 组参会人变化合并为 49 个内容页面。生成层与展示层都清洗 `<think>/<analysis>`，结论审计沿用稳定事件 ID。
- MeetingPack v4 将顶层收敛为 Viewer/README/assets，并同步在线“会议纪要 / 章节脉络 / 屏幕内容”、完整逐字稿、屏幕缩略图/VL 详情和证据跳转；默认直接打开最重要的会议纪要，不再让收件人在管理层/执行层与快速/精细四种重排视图中选择。导出前统一清洗 VL 的 `<think>/<analysis>` 并生成稳定页面标题，清洗结果同时进入 evidence、RAG 与 Viewer。页面导出为 960px WebP，音频为 AAC 40kbps，视频为 H.264 720p/10fps 分享版，项目母版不被修改。当前 Gate B v4 实测为无媒体 2.36MB、音频 23.43MB（原 PCM 149.55MB）、视频 40.10MB（原视频 206.92MB）。导入固化从硬链接升级为独立 inode 的 CoW reflink（失败回退复制），成功上传自动清理 inbox；新增 `meeting-storage/v1` 与白名单智能清理，只回收 PCM、VL full 帧和 RAG 索引。
- 带录屏管线改为渐进式纪要：转写/说话人完成后立即发布语音草稿，VL 完成后原位升级多模态终稿，由 `meeting-generation/v1` 记录阶段/revision/补充数量。前端自动打开草稿并保持标题阅读锚点；草稿允许播放、翻译和追问，但服务端禁止编辑、结论审计、Topic Map、重生成和导出。页面判定进一步抑制稀疏红框/激光点并增强顶部大标题；合成回归覆盖参会人栏、标注和同表格换标题。当前 Gate B 录屏旁路验证为 4674 帧 / 87 稳定段 / 51 逻辑页 / 11 摄像头段，未覆盖 canonical 资产。
- 修复真实 Quarterly PLM 处理中的渐进发布失效：语音草稿使用的旧 `summarize.py` 未关闭 Qwen thinking，8192 token 全部进入隐藏推理后正文为空并返回 `rc=3`，导致界面只能等 VL 终稿。现在请求显式 `enable_thinking=false`，错误行进入受控作业日志，并新增独立请求回归；会议列表、状态条和纪要区按 `has_minutes + voice_draft_rc` 区分“草稿可读”与“草稿失败，生成终稿”。正在运行的真实 VL 作业不并发补写同一会议的 minutes/evidence，避免 revision 竞态。

## 部分完成

- 纪要版本：助手修改已有 `.history/minutes/` 与一步撤销，重新生成保留一份 `minutes.prev.md`；尚无统一版本清单、任意版本比较/恢复和 evidence 配套版本。
- RAG：单会议 embedding/reranker、revision 索引、降级和检索审计已完成；尚未建立跨会议查询、多人鉴权和会议级权限边界。
- MeetingPack Viewer：离线阅读、媒体跳转、完整逐字稿、证据状态，以及会议纪要优先的三层阅读结构已完成；旧会议不会凭空生成 claim，若要得到完整结论视图仍需用当前 prompt 重新生成 evidence。尚未完成真实收件人的可用性试验、打印/PDF 样式和无障碍专项检查。
- Schema：Prompt、evidence、RAG 与 MeetingPack 已有显式版本；Web `/api/...` 仍未版本化。
- 人工验收集：单会议标注界面、`meeting-minutes-evaluation/v1` 本地事件格式、问题分类、汇总和失效规则已完成；尚未由用户完成首轮真实会议验收，也未实现跨会议、模型版本与 prompt 版本的对比汇总。
- 翻译：逐字稿中英目标语言、原文/译文/对照阅读与整场上下文缓存已完成；尚未经过真实中文转英文、英文转中文和中英混合会议的用户旅程验证，也未实现公司/会议术语表、单轮人工改译、选择性重译和多语言纪要版本。
- 服务部署：user systemd 重启通常可用，但本次上线时旧 Uvicorn 在 `active_jobs=0` 下仍因未关闭的 HTTP 连接等待退出超过 45 秒；确认无活动作业后强制结束旧 Web 进程并成功启动新版本。尚需给优雅退出增加有界超时和连接排空状态，避免未来部署无期限等待。

## 仍需推进

### P1

- 抽出 `meeting_core`，把 `web/server.py` 拆为版本化 meetings/jobs/speakers/orgchart/assistant/quality/export routers 与 services；旧 `/api` 保持兼容。
- 给所有既有纪要生成路径统一接入多版本历史和版本浏览，并让 minutes/evidence 成套比较与恢复。
- 用 3–5 场代表性会议完成首轮真实人工验收，汇总引用准确性、决定/提议误判、虚构负责人/期限和无证据内容，再决定 prompt/model 调整；不得直接用标签自动改写正式纪要。

### P2

- 助手支持 SSE 流式响应、停止生成、稳定来源编号和“检索/核对/组织回答”阶段状态。
- 用真实会议验证 Chapter 边界和重复页面返回场景，继续校准 Topic/思维导图的数据模型；在线与离线统一“纪要优先、脉络理解、屏幕核查”的信息架构。会议库增加重复候选、回收站、项目/标签和待核对筛选。
- Web 服务增加有界优雅退出；区分普通浏览连接、同步 LLM 请求和后台作业，部署时明确显示等待对象并在超时后安全退出。
- 支持纪要选区作为明确修改目标，以及逐字稿人工修订和“纪要已过期”状态。
- 根据真实试用反馈补充翻译术语表、语气/术语错误反馈和单轮人工改译；在确认阅读交互前不扩展到多语言纪要重写。
- 在现有单会议混合 RAG 上设计跨会议检索范围、身份鉴权、会议级 ACL 和索引删除/撤销语义；在这些边界完成前不提供全局搜索入口。
- 为删除会议提供回收站/恢复流程；当前仍是确认后直接删除。

### P3

- 渐进迁移详情前端到 Vue 3 + TypeScript + Vite；引入组件/状态边界和 Playwright 用户旅程测试，MeetingPack Viewer 保持静态单文件。
- 完整键盘导航与无障碍检查；响应式基础已覆盖 1600×900、1366×768、1024×768 和 390×844，但仍需用自动化用户旅程持续回归。
- 作业持久队列与服务重启恢复。
- 完成 Web API schema 版本化，并增加更细的性能/资源指标。
- 在 ACL 和删除/撤销语义完成后再做 typed decision/action/risk、跨会议 Inquire 和只读 MCP；不先开放无权限边界的全局搜索。

## 下一次评审触发点

按当前顺序，在以下任一节点完成后更新本文：阅读连续性/状态条/可调分栏上线、前端或 API 模块迁移开始、逐字稿翻译真实试用反馈、首轮真实验收与问题汇总、统一纪要版本浏览、逐字稿人工修订与纪要过期状态、会议回收站、本地跨会议 RAG。若期间发生真实环境数据或播放事故，应先记录事故原因、恢复方式和新增回归，再重新评估优先级。
