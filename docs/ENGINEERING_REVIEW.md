# 工程走查记录

- 首次走查：2026-08-10
- 最近更新：2026-08-26
- 状态：持续维护

## 2026-08-21：终稿统计收尾误伤正文

- 长会议第二轮身份一致性重试触发“待办证据定点修复”后，模型 token usage 同时包含整数计数和嵌套 `prompt_tokens_details`。旧代码遍历所有键并对值执行 `int()`，因此正文/修复稿已经返回后仍在统计合并处失败，上传卡片误显示“生成纪要失败”；现存语音草稿的独立翻译任务随后运行，又造成流程跳步错觉。
- 修复只累加非布尔 `int/float`，嵌套诊断明细不参与业务统计；回归用虚构嵌套 usage 强制经过初稿、重试和待办定点修复完整路径。草稿翻译作业另显式标注来源状态，但继续保持独立 revision sidecar，不覆盖 canonical 纪要。

## 2026-08-26：公开媒体入口与前端单文件边界

- 媒体入口若直接把 URL 和下载器返回值塞进作业/会议 JSON，会同时泄漏签名参数、Cookie、请求头、本机路径，并让列表、Viewer 与 KB 各自形成来源规则。现改为私有 inbox 请求 + 受限下载器 + `media-source/v1` 白名单；内容类型和来源标题在语音草稿前发布，失败恢复也不会误走会议 prompt。当前只校验初始 DNS/IP；在服务开放到 LAN 之前，下载 worker 仍需进一步做网络命名空间/出口代理隔离，以覆盖恶意重定向和 DNS rebinding，不能把当前入口当公网抓取服务。
- `web/static/app.js` 已约五千余行，长度本身不是故障，但导入、库、队列、播放器、逐字稿、纪要、来源和导出共享可变状态，新增需求需要扫描过多无关代码。决策是保留模块化单体和原生 JS，按领域抽 ES modules，让 `app.js` 收敛为装配入口；Meeting/Media 只做 domain profile，不复制分析 core 或页面。现有八个无 DOM 规则模块负责来源、导入、任务、内容库、核听、逐字稿、导出和纪要状态；`transcript-view.js` 已作为首个 DOM projection 接管逐字稿节点创建、长轮次续段、译文/复核态和滚动锚恢复。它只接收显式数据和 callback，不读取全局 `state`、不请求 API、不控制播放器，因此声纹绑定、引用、修正与 seek 的副作用仍由入口 controller 决定。纯规则与模块导出用 Node 回归，真实装配和浏览器模块加载用 Headless Chromium 回归。下一阶段按同一契约迁移纪要 DOM renderer；继续保持 API、页面和 Viewer 行为，暂不以 React 或微前端重写制造第二套状态系统。

## 2026-08-21：整阶段门阻塞核听导出与急件

- 原导出 API 把“纪要终稿 ready”作为整个 MeetingPack 的准入条件；实际用户急需的是说话人逐字稿和核听跳播，而这些资产在 VL 之前已经形成。运行队列又只有等待项重排，长 VL 任务一旦开始，急件只能取消并靠人工判断如何续跑，存在重复计算和丢失进度的风险。
- `4d28155` 将能力门拆开：逐字稿非空即可导出明确标注的处理中核听快照，缺失纪要由代码提供占位说明，不调用模型；正式包继续使用同一 v5 schema，消费端通过 `document.state/snapshot` 区分。调度侧只允许处于安全后半程、已有逐字稿与页面清单的 upload/regen 暂停，并在杀进程前构造受控续跑命令；急件之后自动续跑，已完成逐页缓存不重算。早期 ASR、说话人和页面提取阶段明确拒绝抢占。
- 验证覆盖无纪要导出、快照标识、暂停/急件/自动续跑队列顺序、早期阶段拒绝和 inbox 延迟清理；`make check` 与隔离 Web smoke 163/163 通过。限制：当前断点粒度仍是阶段/逐页缓存，不保存模型单次请求内部状态；在不安全阶段只能普通置顶，等待当前阶段结束。

## 2026-08-17：Topic Map v3 导航契约

- 修复“一字段两职责”：旧实现让一级 Topic 的 `turn_ids` 同时承担“证明观点的代表证据”和“播放器应聚焦的完整范围”。reduce 合理地压缩代表引用后，UI 就误以为其余内容未覆盖；若用邻近补齐，又会伪造语义归属。
- 新契约以稳定 `candidate_id` 保存 map→reduce 吸收关系，代表证据不扩写，另生成互斥 `navigation_segments` 和 `navigation_turn_ids/ranges`；过渡与未知分别可见。覆盖率按 turn 计算并单列发言秒数，避免把停顿误判为内容缺失。
- 在线与离线 Viewer 共用这套导航语义，v1/v2 保持只读兼容。回归必须同时断言：代表证据没有被导航污染、候选覆盖可以恢复、未知内容没有被自动塞进 Topic。

## 维护约定

本文是工程评审台账，不是一次性报告。出现以下情况时必须同步更新：

- 完成影响产品交互、数据格式、生成策略、隐私边界或运行架构的改动；
- 修复真实环境事故，或发现合成测试未覆盖的失效路径；
- 某一“仍需推进”事项完成、部分完成、取消或优先级改变；
- 开始新的大型能力之前，对依赖项、数据安全和回归范围进行复核。

更新时记录结果、对应 Git commit、验证方式和仍然存在的限制；完成项移入“已处理”，只有基础设施已具备但用户闭环尚未完成的事项标为“部分完成”。评审文档不得写入真实逐字稿、纪要正文、人员名单、凭据或私有原始路径。

## 已处理

- **同场匿名声纹发生多对一坍缩（2026-08-21，`6b52345`）**：原入库循环一边匹配、一边把新匿名 voice 写回同一候选库，后续相似的 pyannote 聚类会把“本场刚新建”误当作已有声纹命中；结果是一条匿名身份包含多个真实发言来源，恢复重跑还可能继续改变映射。修复为冻结会前候选、未绑定 voice 同场单聚类、已绑定 person 才允许多簇，并保存唯一 source+raw-cluster 映射用于幂等与历史坏数据收敛。会议删除和碎片清理同步清映射。合成回归覆盖空库相似簇隔离、旧坍缩拆开、已确认人员多簇、重复运行与来源清理；`make check` 及隔离 Web smoke 161/161 通过。限制：它修复声纹入库映射，不承诺恢复单流 ASR 在真正重叠说话时没有识别出的第二路文字。
- **ASR 已完成却在“区分发言人”失败（2026-08-21，`5796d2d`，兼容补丁 `0d18688`）**：真实长视频已完成 ASR、字级对齐并写出 `stamps.json`，但重构后的逐字稿 Markdown 投影仍访问旧变量名，触发 `NameError`。这不是 ASR/pyannote 模型故障，而是运行时投影路径缺少合成回归。修复为单一确定性落盘函数，并把完整 stamps 提升为受控检查点：视频母版、音频与 stamps 均完整时，失败卡可“从说话人识别继续”，跳过 ASR、重建文本投影并重跑 pyannote 及后续阶段；资产不全时拒绝伪恢复。首次部署还暴露 p80 前 Web 预测 slug 会清掉 `_Meeting_Recording`、视频脚本却保留该后缀，导致检查点存在但恢复器找不到目录；新上传已统一两端规则，历史作业只允许固定后缀的精确回退，后续任务绑定实际目录名，禁止模糊匹配。现场复核继续发现，前端会在失败一小时后无条件隐藏卡片，使可恢复任务失去入口；p81 改为可恢复失败持续显示至续跑成功，不可恢复失败仍限时收起。新增纯合成落盘测试、恢复规划/API smoke 与静态交互断言；`make check` 与隔离 smoke 通过。限制：当前 pyannote 结果尚无独立检查点，因此说话人分离仍需重跑。
- **MeetingPack 屏幕图重复派生且不便取用（2026-08-21，`5796d2d`）**：旧导出为 Viewer 另生成 1600px WebP，既重复计算又不适合直接拖入 PPT/Word。现 `assets/slides/pNNNN.jpg` 逐字节复用 VL 原生分析 JPEG；缓存被回收时从受保护视频母版按同一时间点和分析参数恢复，旧非 JPEG 逻辑页才做兼容转换。manifest 声明来源与 MIME，README 给出取用方法及 P 证据边界。151 页样本图层由 8.66MiB 增至 36.78MiB（+28.12MiB，约 +325%），是保留原分析分辨率与办公软件直接使用的显式体积取舍；导出预检已改用真实帧或保守恢复估算。

- **ASR 特殊能力与单一供应商耦合（2026-08-19，`9d25c13`）**：原转写入口直接 import Qwen/PyTorch，并默认所有 ASR 都支持原生 context 与 forced aligner；一旦迁移本机服务、局域网服务或云端就只能重写管线，也没有“端点不支持 Context”与“服务故障”的不同降级语义。现由 `meeting_core.asr` 收敛为 native 与 OpenAI-compatible provider，字级时间戳是显式能力合同；Context 在 auto 模式只做同端点无 prompt 重试，跨 provider 回退必须配置，默认不允许静默上云。确认术语混淆只重听有时间证据的短音频，有二次声学支持才自动改 canonical；失败保留第一遍结果。人工修正使用 revision、快照和 stale 下游闭环。虚构回归覆盖兼容端点、能力缺失、显式 fallback、定点复核、编辑/撤销与音视频旧会议重转写；`make check`、Web smoke 158/158 通过。仍有限制：v1 尚无通用声学低置信分数与本场早期屏幕 OCR 选词，领域化术语池仍待实现。
- **会议库时间与错误外部逐字稿纠错（2026-08-18）**：列表原按目录 slug/来源日期排序，且视频 + VTT/DOCX 无条件采用外部正文。现将首次导入时间与最近派生资产更新时间原子写入 `meta.json`，旧会议从 upload job/派生文件估算，不读保留的源 mtime；前端默认最近导入并保存三种排序。上传 API 新增显式 `ignore_transcript`，bundle 新增 `transcript_source/external_transcript_available`；存量重转写进入与新会议同级的串行作业，覆盖前快照派生资产、失败恢复，母版和外部逐字稿不删除，屏幕/VL 不重跑。虚构数据回归覆盖导入路由、API 作业、快照/恢复和前端契约。
- **同一资产中的待办语义与议题归属发生矛盾（2026-08-17）**：纯音频路径曾允许“待办事项：无”与议题正文里的 `kind=action status=confirmed` 同时存在，导出侧又把机器 T 主键直接呈现给读者；Topic Map 的兜底还会把长段未理解内容硬塞给最近标题，并让同一轮次属于多个一级议题。根因不是单个 prompt 文案，而是语音草稿绕过共享输出护栏、展示层与机器 linkage 未分层、覆盖率被当作必须凑满的 UI 指标。真实回归又发现 Topic Map 的合法 JSON 在解析前误用了 VL 人读清洗器；该清洗器会删除独占一行的 `{`/`}`，代码因此亲手破坏 JSON，再让 LLM 反复“修复”并耗时数分钟。修复为：直出/map-reduce/语音/多模态共用待办校验与定点修复；人读层隐藏主键、sidecar 保留；一级议题互斥归属，长未知区间如实留空；模型清洗拆成“只去 reasoning、保留数据语法”和“VL 展示包装清理”两层，map/reduce 再用 grammar 强制 JSON，代码继续验证 schema 与引用。教训：内容协议必须在所有生成入口统一执行；结构化数据绝不能复用会改写标点/包装的人读清洗器；覆盖率是认识边界，不能用错误归类换取漂亮数字。
- 屏幕标题 VL 包装泄漏（用户报"屏幕内容标题又乱了"，真实会议 69 页约三分之一异常）：模型输出三种新包装形态——LaTeX `\boxed{…}`、反斜杠转义 `\## 标题`、JSON 片段 `"标题": "…",`——清洗边界只处理 `<think>/<analysis>`，标题提取直接采到 `\boxed{`；角色/价值正则不容忍引号也退回启发式。修复全部落在读路径清洗层（`clean_model_text` 拆 boxed/还原转义/去孤立花括号，`visual_title` 支持 JSON 键形态，role/value 正则容忍引号），存量会议与导出包免重跑 VL 自愈。教训："模型输出清洗边界"要按形态家族持续加固（reasoning 标签 → 特殊 token → LaTeX/转义/JSON），每出现一种新包装就补一条确定性规则 + 一个合成回归，不要指望 prompt 能约束住。真实 69 页验证异常清零，合成单测两形态，`make check` 与 123/123 隔离 Web 回归通过。
- 直出路径绕过生成护栏（用户报"新传的会议纪要那么短"，真实 77 分钟会议）：排查发现 `overview_mode=direct`（上下文未超限，单次直出），而退化检测/必需章节校验/待办合规校验全部只挂在 map/reduce 路径——直出稿总体章节零 evidence marker（103 条 claims 全在分页详情），依据链接消失、正式待办投影为空、阅读版仅 2182 字。修复：`generate_direct` 与 map/reduce 共用 `_complete_with_guard` + `_repair_todo`（后者抽出共用，notes 传完整上下文）。教训：同一份输出的"两条生成路径"（直出/分段）必须共享全部质量护栏，给长路径加的防护不会自动保护短路径；本次事故正好是 65k 上下文让 77 分钟会议首次落进直出分支后暴露的。另注意测试接缝：直接把直出切到 `LocalLLMClient` 会让 stub `chat` 的旧测试静默打真实模型（minutes_policy_test 就是靠断言拦住才暴露），路径级调用必须经模块级可替换接缝。合成单测四路径 + `make check` 与 123/123 隔离 Web 回归通过。
- 正文"依据"链接被反引号吞掉（用户报"依据的跳转没了"）：该次重生成的模型把证据标记写成 `` `<!-- mm:evidence … -->` `` 反引号包裹形态，`markdown_with_evidence_links` 替换后留下 `` `[依据](#mm-…)` ``，markdown-it 渲染成 `<code>` 纯文本而非锚点，前端 `#mm-` 点击处理整体落空（待办表格走 `_grounded_actions_table` 直写链接故正常，加剧排查迷惑性）。修复：新增 `WRAPPED_MARKER_RE`（`` `? `` 可选包裹 + 仅空格级空白，不吃换行）连带剥掉反引号，Web（deps.py）与导出（export_meeting.py）共用同一函数同步愈合；存量会议免重生成。教训：证据标记是"模型手写、代码解析"的协议，模型会发明各种包装（反引号/独占表格列），所有 marker 消费点都应按"可能被任意 Markdown 语法包裹"做防御。合成单测覆盖两种形态，真实 PLM 纪要验证 32 链接全渲染、零残留，`make check` 与 122/122 隔离 Web 回归通过。
- 待办表格"marker 独占状态列"渲染错乱（真实 PLM 会议用户截图报"事项里一堆竖杠、负责人期限全是待确认"）：模型输出形如 `| 事项 | 负责人 | 期限 | <!-- mm:evidence … --> |`，`_action_fields` 剥离标记后全表过滤空单元格，4 列塌成 3 列落入纯文本分支，事项格残留竖线、owner/deadline 丢失由渲染层默认"待确认"填补，唯状态列（claim_status 映射）正确——症状与截图逐格吻合。修复：`_action_fields` 只丢尾部空单元格并新增 3 单元格分支（事项/负责人/期限，状态交 claim_status 兜底）；`action_items_from_claims` 对旧 sidecar 已误判 claim（owner 空且 text 含竖线）用原文重拆，旧会议免重生成即生效。教训：写路径（parse_claims 存 claim["action"]）与读路径（投影时重拆）都可能遇到同一份脏数据，修解析器必须同时给存量 sidecar 留重拆兜底。合成单测覆盖新解析与旧 sidecar 两路径，真实 PLM evidence.json 验证 7 条待办全部正确拆列，`make check` 与 122/122 隔离 Web 回归通过。
- 待办证据标记合规校验 + 定点修复（用户两轮报"两小时会议没有有依据的待办"）：第一轮根因是 reduce 退化循环烧光输出预算（见上条防护）；换码重生成后第二轮根因暴露——模型把 9 行待办写进表格但全部不带 `kind=action`+`turns=` 标记（标记散落在关键结论/风险章节），正式待办只从有依据 claim 投影故前端为空。修复：`_todo_compliant` 校验挂进 reduce 的 `_complete_with_guard`（有表格行就必须逐行带合规标记，"未形成明确待办"除外），不合规先随防护重试，仍不合规走 `REPAIR_TODO_PROMPT` 定点修复轮（只按片段笔记重写待办章节）+ `_splice_todo_section` 拼接回终稿；修复也不合规则保留原稿并 stderr 告警。合成数据单测覆盖校验/重试/修复/拼接全链路。
- 间距 token 缺口致全站间距归零（用户报"纪要总体摘要贴着 border""脉络太丑"）：token 化只定义 `--sp-4…24`，全站 15 个大值（--sp-27…90）引用无回退、整声明在 computed-value 阶段作废，padding/gap 静默归零。补齐定义 + `var(--line)` 误用改回 `--border`；新增 `design_tokens_test.py` 防线（无回退 var() 必须可解析 / 字号零字面值 / 花括号平衡）入 `make check`。脉络树顺手重构为通栏：根节点横条置顶、分支通栏、左轨线内收，消除左列死区。教训：token 化这类机械替换必须配"引用可解析"静态检查，截图走查查不出"刚好是 0"的间距。
- 智能栏遮挡与浮层层叠修复（用户报"脉络/纪要进度条没了""hover 浮层被灰边框盖住"）：①`<1500px` 智能栏 absolute 覆盖右栏右缘 430px 含滚动条，1051–1499px 区间 `.utility-open` 时 review-grid 右移 430px；②`#tl-tip` 嵌在 `z-index:3` 的 `.player-box` 层叠上下文内被 `z-index:4` 的 `.pane-resizer` 压住，移至 body 直属。用一次性 DOM 探针（?probe=1，测完即删）实测三个滚动容器 overflow 链路正常，AI 对话滚动条在当前代码确认存在。另支持 `?meeting={slug}` URL 直达。
- 总体纪要退化失控防护（真实事故：2 小时 PLM 会议待办章节整体消失）：根因是 reduce 阶段模型陷入"自我修正"循环，同一长句反复重述烧光 6144 token 输出预算，排在最后的待办章节被截断，evidence 解析不到任何 action（claims 85 条正常、actions 0）。防护分三层：`minutes_overview._is_degenerate` 检测（同一 ≥40 字长行 ≥4 次，或自我修正链标记 ≥4 处）→ `repeat_penalty=1.2` 完整重试一次（`LocalLLMClient.complete` 新增透传，llama-server OpenAI 接口原生支持）→ 仍退化则 `_clean_degenerate` 确定性清理（重复长行留首现、自我修正链整行删）后继续并 stderr 警告，不再把循环垃圾写进 minutes.md；reduce 缺"## 总体摘要"/"### 待办事项"章节同样触发重试，map 片段共用同一防护。合成文本单元回归覆盖检测/重试/清理/截断四路径，`make check` 与 122/122 隔离 Web 回归通过。遗留：出事故的会议需在新代码上重新生成纪要恢复待办（旧 minutes.md 是失败作业产物）。
- 提速与流式批次（用户点名"生成速度"方向）：①VL 逐页解读串行改有界并发——实测 2 小时会议 VL 占 24.6/52.5 分钟为最长阶段且 `describe_pages` 原为逐页串行 HTTP 循环；现 `ThreadPoolExecutor`（`MEETING_VL_WORKERS` 默认 2）+ `meeting-vl` 服务 `--parallel 2 --ctx-size 32768`（每槽 16k，本机 78GB 可用内存实测无压力），缓存仍为每页完成即原子落盘、中断续跑语义不变；stub 服务单元回归验证并发生效（5 页 1.0s < 串行下限 2.1s）、空正文页仍走紧凑降级且不进成功缓存。真实 VL 提速幅度待下一次真实会议处理确认（GPU 饱和时收益会打折）。②助手 SSE 流式：`assistant_service` 拆出 `prepare_answer`（校验+检索同步完成，冲突在流开始前 409）与 `stream_answer`（`_chat_stream` 逐段读 OpenAI SSE），新端点 `/assistant/chat/stream` 帧序 meta→delta→done，前端逐段填气泡、done 后全量重渲染接回引用链接与 localStorage 持久化，中途失败撤掉空气泡；原端点保留不动。冒烟新增 2 条流式断言。③碎片声纹自动合并：入库收尾时 ≤2 轮未绑定声纹以 0.80 高阈值再匹配并入，单来源碎片删条目删向量；合成库单元回归覆盖命中/正交不命中/已绑定不动/幂等。④Viewer 模板无头启动回归（Chromium 缺失自动跳过），防 `node --check` 拦不住的 TDZ 类运行时错误——正是当天导出空白事故的根因类别。遗留：文本路由仍 `--parallel 1`（65k 上下文分两槽会切破长会大窗口，需先评估再加）；重启超时退出问题（P2）在本次部署中再次出现，仍待有界超时改造。
- 声纹拆分升级为半监督重排 + 碎片声纹整体改派（用户两轮实测反馈）：①长会议不可能逐条标记错分轮次——拆分从“只处理标记轮次”改为以标记轮次（一条即可）建立新簇质心，再把该声纹本场其余全部轮次的嵌入与新旧质心做 argmax 余弦重排（`voice_enroll.reassign_by_centroids`），质心与匹配均用重排后的扩展成员计算，原声纹质心用重排后剩余轮次重算；重排后原声纹不剩轮次时 400 拒绝（校验在写库前）。②用户标记 16:22 单轮被拒“全部轮次”：排查发现该轮属于仅 1 轮的同名碎片声纹（867 轮会议 20 条声纹中 2 条仅 1 轮，显示名与 150 轮大声纹相同，用户视角无法区分），拆分本不适用；前端现在识别“该声纹轮次被全部选中”，填“具名为”即直接调 `/bind` 整体改派，未填则提示“只有 N 轮无需拆分”，不再发出注定 400 的请求；服务端该 400 文案同步改为可操作指引。响应新增 `reassigned` 计数。合成向量单元回归新增 4 例（样例带动重排、全部留守、多簇、空输入），`make check` 与 120/120 隔离 Web 回归通过。遗留：真实会议的自动重排准确率待用户实操目测（若重排过度可考虑加相似度裕度阈值）。
- 声纹拆分可操作性与一步具名（用户实测“标记后没反映”）：排查服务端访问日志确认从未收到 `/split` 请求，前端标记、入口与轮次声纹覆盖（PLM 867 轮中 697 轮有 voice）均正常，问题定位为最后一步可发现性差 + 静默失败路径（无有效声纹分组时静默清除标记、网络异常无提示）。修复：底部操作条改醒目橙色常驻条，文案明示“已选 N 轮 → 点右侧「确认拆分」生效”，按钮改名“确认拆分”加底色；新增“具名为”输入框（复用 `#person-list` datalist），填入姓名则拆分后对每条新声纹自动调 `/bind` 一步具名（`create:false`，未精确命中时 toast 引导手动具名）；`applySplitMarks` 包 try/catch/finally，失败保留标记可重试。验证：`make check` 与 120/120 隔离 Web 回归通过；真实会议拆分效果待用户实操目测。
- 设计系统 token 化（P3 原子化第一阶段，用户需求：换主题/组件风格只改 token、页面不崩）：三层合同落地——语义色层（`--bg/--ink/--accent` 等）、阶梯层（`--font-sans/--font-mono`、`--fs-8…22`、`--fw-400/500/600`、`--sp-4…24`、`--r-2…18`、调色板 `--topic-1…8`/`--speaker-1…8`）、组件层（`button`/`input`/`.chip`/`.mode-tab`/`.dialog` 的 `--x-*` 变量均带回退值）。`style.css` 全站 font-size 字面值清零（181 处），spacing/radius/weight 全部变量化；JS 双轨填色（`app.js` `palettes()`、Viewer `_palRead()`）经 `getComputedStyle` 读同一组调色板 token 并带兜底。新增 `theme.css` 主题插槽（两个页面均在 style.css 后引入，注释模板含全量可覆盖清单与浅色主题示例），定制合同写入 `docs/DESIGN_TOKENS.md`。边界保持诚实：token 覆盖外观，栏宽/栅格/DOM 结构仍需改布局代码，模板化/Vue 3 迁移仍在 P3。同批修复窄屏逐字稿姓名 chip 靠右（`.turn` 三列网格 + chip `justify-self:start`）。验证：`make check` 与 120/120 隔离 Web 回归通过，桌面/窄屏截图走查无视觉回归。
- 录音笔音频管线补齐声纹入库：根因是 `bin/run_all.py` 只做转写+分离，从不写 `voice` 字段，导致录音笔导入的会议在网页端无绑定入口（真实 60 分钟会议 378 轮全部 `voice=null`）。新增 `bin/voice_enroll.py`：复用 `MEETING_PYANNOTE_MODEL` 下的嵌入子模型，整音频一遍滑窗推理后按 `diarization.json` 段求每说话人质心，与 `video_minutes.enroll` 同语义匹配声纹库（0.70 阈值，命中跨会议具名，未命中入库待绑定，占位名不自动建 person），原子回写 `transcript.spk.json` 并重生成 `transcript.spk.md` 与试听片段；`run_all.py` 接入“[2.5/3] 声纹入库”（check=False，失败只警告）。回填该真实会议：4 条新声纹入库、1 条跨会议命中、378 轮全部获得 voice。另修复纯音频会议（无 `slides.json`）在网页端重生成纪要秒败（真实作业 rc=1）：`regen_minutes` 按有无分页资料分流，纯音频改走与 run_all 相同的 `summarize.py` 整场管线，且不支持 `--refine-model`（400 明确提示）。
- 声纹按轮拆分（真实用户需求：两位声音相近者被并入同一声纹，原绑定/改名只能整声纹改派）：交互经用户走查定为绑定弹窗入口——“有些轮次不是 TA → 逐轮标记拆分”进入标记模式（`state.splitTarget`，该声纹轮次高亮可点、整轮点击改为标记/取消，底部操作条常驻计数与确认，切换会议清空），不做每轮悬停按钮（噪音大、低频）。服务端 `POST /api/meetings/{slug}/split` 只对所选轮次音频段重提嵌入（`voice_enroll.embed_ranges`，`Inference(window="whole")`、短段零补齐 0.4s），贪心余弦聚类（0.70，质心运行均值）容纳“所选轮次实际是多个人”，逐簇经 `_match_voice_excluding` 匹配库内其他声纹（排除来源声纹防回流）或匿名 `add_voice` 待绑定，只改派所选轮次；原声纹质心用剩余轮次重算覆盖 `.npy`，避免污染延续。嵌入提取在 `BANK_LOCK` 外执行；空轮次/跨声纹/整声纹拆分在校验阶段 400，不加载模型。验证：合成向量聚类单元回归 5 例，冒烟新增 4 条负路径断言，`make check` 与 120/120 隔离 Web 回归通过；真实会议的拆分效果待用户实操目测。
- 在线端改名与逐字稿搜索、Viewer 批量修复：`_meeting_identity` 支持 `meta.json` 的 title 覆盖；新增 `POST /api/meetings/{slug}/rename`（复用 `_mdir` 防穿越，tmp+`os.replace` 原子写），前端就地编辑乐观更新失败回滚；逐字稿搜索为纯前端包含匹配（命中高亮、Enter/Shift+Enter 跳转、渲染后按原命中位置重标记不滚动）。修复 `loadedmetadata` 用媒体时长重建时间轴导致短媒体夹具议题块出屏（取媒体与会议时长较大者）。Viewer：三栏宽度重排、屏幕舞台 190→320px、搜索结果区 `flex:0 0 auto` 修复被压没、独立聚焦蓝条删除改为叠加进 Topic 车道（冒烟断言同步）、scrub 拖动中 timeupdate 不回写修复回弹、窄屏逐字稿 52vh 上限修复消失。验证：`make check` 与 116/116 隔离 Web 回归通过，headless Chromium 桌面 1600px 与 390px 截图走查通过。

- 完成时间轴“Topic 全覆盖车道 + 说话人泳道 + 人物 Focus”重构（commit `003c2f7`）：上层一级论点区间铺满整场，未覆盖时间以灰色间隙块填充（点击只 seek），使 Topic 提取的覆盖缺口在 UI 上可见；下层相邻同说话人轮次合并成色块，按首次发言顺序从独立调色板确定性取色，悬停 tooltip 显示该窗口发言构成，双击展开 ±60 秒轮次级区域放大镜（关闭按钮 / Esc）。时间轴下方人物图例进入 person 模式 Focus：高亮而非过滤（泳道降透明度、逐字稿 `focus-related`、focus-summary 显示轮数与相关结论），与 overview/topic/time 共用 `updateFocusPresentation` 管线；说话人颜色在图例、泳道和逐字稿 chip 左边框间一致，全部从 canonical `transcript`（speaker/start/end/voice）确定性派生，无新资产、无 `/api` 变更。同时完成：逐字稿整轮点击 seek（`window.getSelection()` 有选中时保留复制）、≤820px 两行轮次与逐字稿工具条折行、译文目标默认值跟随界面语言（手动选择写入 `translationTargets` 后不再跟随）。离线 `meetingpack_viewer.html` 在 scrub 条下方同步双车道与人物图例，Focus 语义一致，保持单文件无依赖。验证：`make check` 与 116/116 隔离 Web 回归通过。遗留：Topic Map 提取质量未动，灰色间隙块先用于暴露覆盖缺口，提取改进需真实会议验证后单列推进。
- 完成 Topic Map 全场覆盖与时间轴二轮：真实 2 小时 PLM 会议反馈 Topic 覆盖不足 50%，根因是生成端只保留模型明确挂接证据的轮次。现在局部归纳要求每轮归入候选或显式列入 `uncovered_turn_ids`，归并必须覆盖全部窗口材料（允许一个 `low_value` “过渡与杂项”论点），剩余未挂接轮次由代码按时间邻接确定性归入最近论点；schema 升 `meeting-topic-map/v2` 并兼容 v1，`stats.coverage` 记录覆盖率，该会议实测 46% → 98.9%。加固：单窗归纳修复仍失败降级为空归纳交兜底分配，归并 max_tokens 5000→8000 且修复失败完整重试一次。时间轴：说话人泳道改像素桶节奏条（桶数与会议长度无关，根除碎片）、删除双击放大镜；人物图例按占比降序配色、前 6 名 ∪ ≥5% 直显、未绑定/会议机灰斜纹沉底带绑定入口、可展开逐人车道（默认 5–6 人）；Topic 车道投影 confirmed/action 菱形刻度；逐字稿长轮次句子边界分块、插值 seek；≤820px 逐字稿列表保底 200px 高。MeetingPack Viewer 同步。验证：`make check` 与 116/116 隔离 Web 回归通过（smoke 的 topic-map 断言随夹具升 v2 同步更新）。遗留：逐人车道与节奏条未经真实浏览器目测；分块只在纯原文模式生效；“未绑定”判定按命名启发式而非 person_id（bundle 未含该字段）。
- 时间轴密度与可读性修复（真实用户走查反馈，菱形刻度方案回滚）：删除 Topic 车道结论/待办菱形刻度及 `revealTimelineClaim`（结论入口保留在纪要，不再占用时间轴）；逐人车道开关从混在人物图例中的 chip 移为车道区块右下角的独立虚线按钮（`#person-lanes-bar`/`#person-lanes-toggle`，收起时也常驻），与人物 chips 视觉区分；节奏条桶间重叠 0.05%→0.2% 且车道加底色，消除密集会议下的桶间白缝；Topic 车道论点块不透明度 .42→.62、low_value .16→.28、灰隙 .05→.09，深色面板上可辨。MeetingPack Viewer 同步删菱形、移动开关、加大桶重叠。验证：headless Chromium 对 80 分钟/430 轮/8 人合成密集夹具截图走查收起与展开两态（未绑定会议机灰纹沉底正确），`make check` 与 116/116 隔离 Web 回归通过。
- 议题调色板与人物交互简化（用户走查反馈）：UI 术语统一“论点”→“议题”（产品文档同步，代码内 prompt/数据字段不动）；新增 `TOPIC_COLORS` 8 色调色板，时间轴议题块内联着色（hover 改 `filter: brightness` 以兼容内联背景）、右侧脉络“议题 NN”标签同色。人物 chip 的“人物 Focus”模式整体移除（`setPersonFocus` 及 `focus.mode==="person"` 分支删除）：改为 `state.speakerHover`/`speakerPin` 轻量隔离——悬停预览、点击钉住，只调暗主时间轴与他人车道，不再滚动逐字稿或改写 Focus 摘要；开关文案“说话人视图 ▸/▾”。MeetingPack Viewer 同步（`focusPerson` 删除，`spkPin`/`spkHover` 同语义）。验证：headless Chromium 截图确认多色议题块与新文案，`make check` 与 116/116 隔离 Web 回归通过。遗留：hover/pin 两态未经真实浏览器人工目测（无头截图无法触发 hover）。
- 完成 `web/server.py` 拆分（P1）：约 1900 行单文件按域拆为 `web/deps.py`（路径/环境常量、锁、共享会议助手）、`web/job_store.py`（JOBS/EXEC、作业持久化、管线与翻译 runner）和 `web/routers/` 下 10 个路由模块（pages/meetings/quality/translations/export/assistant/media/speakers/orgchart/jobs），`server.py` 收敛为约 55 行装配入口；入口命令、路由路径、请求/响应 schema 与处理逻辑逐字节不变，新旧路由表 50 条逐条 diff 一致。完整 `make check` 与 116/116 隔离 Web 回归通过（commit `8321f2a`）。遗留：`/api` schema 版本化仍在 P3，前端 `app.js` 单文件迁移 Vue 3 仍在 P3，纪要生成路径向统一 LLM 客户端的迁移继续按既有节奏推进。
- 修复真实长会议“待办无论据、议题板块散乱”：该次总体合并输出 25 行待办但全部缺少 evidence marker，同时输出 23 条不符合前端契约的议题文本；canonical evidence 中只有 5 条行动具备逐字稿依据。Web 与 MeetingPack 现在从 evidence actions 确定性重建“可核验待办”并提供依据跳转，原 25 行不删除而进入默认收起、明确不计为正式任务的 `action_candidates`；常规纪要不再重复显示模型议题列表。生成 Prompt 增加硬数量上限，长会 map 目标从 22k 调为 38k tokens，Topic Map 处理窗调为约 15 分钟且局部/全局坏 JSON 都可安全修复。合成单元测试、完整 `make check` 和 103/103 隔离 Web 回归通过。
- 修复真实超长录屏会议“36/36 页 VL 完成后仍失败”：总体终稿请求约 88,166 tokens，超过本机 65,536 上下文；四个逐页分组最高约 39,575 tokens，未超限。新 `meeting_core.minutes_overview` 只对总体部分执行保留 T/P ID 的 map/reduce，短会维持直出。Web 重生成携带原视频、复用缓存并只补空 VL 页，成功后正式 finalize 并刷新 Topic Map，避免子进程完成但文档仍停留 processing。
- 修复真实会议屏幕页被误标“低信息”：该页包含表格、follow-up 和负责人，但 VL 生成日志有 token、reasoning 清洗后的缓存正文却为空；旧逻辑又以“说明少于 70 字”直接判 low，并把空字符串当成功缓存。现在空/缺失结果为 `unknown/待解析`，旧空缓存会补算且失败后不落成功项，短说明不再成为 low 依据；页面价值与是否有讨论继续分开。隔离测试覆盖补算成功、连续空正文和旧缓存迁移，完整 Web smoke 为 103/103。
- 语音草稿模型调用完成首轮边界重构：`meeting_core.llm` 统一 loopback、thinking、超时和错误分类，`context_budget` 统一实际上下文预算，`voice_draft` 对长会议执行保留稳定 T ID 的分段事实提取与全局合并。真实事故中 867 段会议的一次性提示为 88,273 tokens，超过 65,536 上下文并返回 HTTP 400；新策略在发送前即切分，隔离测试覆盖短会直出与长会分段。Topic Map、翻译、助手和多模态终稿尚待逐步迁移到统一客户端。
- 增加根目录 `CHANGELOG.md`，回填本地基线以来全部 40 个提交的可读索引；Git 继续保存逐行完整历史。当前尚未配置 GitHub 远端，真实会议/录音/人员/声纹/组织架构/作业数据已由 ignore 边界隔离。
- 记录未来企业同事受控接入边界：身份与会议 ACL、上传隔离/配额、带 owner 的公平队列、loopback LLM 代理、TLS/会话保护、操作审计和存储生命周期必须先于任何 LAN 暴露；当前服务仍只监听回环地址。
- 建立 Git 基线，并通过 `.gitignore` 隔离全部真实会议、录音、声纹、组织架构和运行状态。
- 分离代码根与数据根，测试/部署可独立指定私有数据位置。
- 将固定共享 `_smoke` 夹具改为每次独立临时根，消除多会话相互删除夹具的问题。
- 上传验证失败时清理半上传目录，并拒绝多音频、混合音视频和不明确的多 VTT 请求。
- 作业 JSON 不再保存任意 stderr，只保存受控元数据；dry-run 结果进入明确的 `result` 字段。
- 增加健康检查、环境 doctor、统一 Makefile 命令和依赖声明。
- 增加本地会议助手：结构化逐字稿引用、来源跳转、问答、纪要修改预览、revision 冲突保护、确认写入与历史备份。
- 将三栏工程控制台重构为会议回顾工作台：左侧播放/时间轴/逐字稿证据栏常驻，右侧阅读纪要，导入/作业/模型入口降级，底部自然语言助手常驻。
- 纪要修改曾采用“保存/继续调整/取消”确认卡，但整篇重组把修改后、完整修改后和修改前三份原始 Markdown 塞进 430px 智能栏，暴露 marker 并形成多个嵌套滚动区。第一轮 v0.9.1 又把整篇重组直接覆盖 `minutes.md`，并错误地把内存态撤销当成用户始终可见的能力。修正后局部修改仍可直接写入并一步撤销；整篇重组只保存为 revision 绑定的 AI 纪要视图，标准纪要始终可切回；`.history/minutes/` 另提供跨刷新恢复入口且恢复前备份当前版本。服务端硬拒绝 document proposal 走章节写入接口。卡片只保留摘要，完整版本按需以安全 HTML 展开，自然语言输入不受入口形式限制。
- 声纹库升级到 v3：稳定人员 ID、国际化类型名称和每人独立首选显示名；近似姓名只给候选，不再自动绑定。
- `/admin` 增加后台声音试听与候选 A/B 试听，并将 Org Chart 改为可增删、拖动改上级、服务端校验环路的图形画布。
- Org Chart 提取不再自动翻译、生成拼音、合并跨语言变体或创建虚假上级；草稿以增量方式进入人工确认流程。
- Org Chart 提取会在解析前剥离模型 thinking/analysis，并拒绝提示词回声行；逐页原始输出保存在私有数据根，便于本机排错但不进入作业日志。
- 回归覆盖近期遗漏的姓名新建、作业取消、上传清理、助手问答/修改、删除与数据根隔离。
- 纪要生成改为结构化 `meeting-minutes-prompt/v1`，用稳定 T/P/C ID 关联结论、逐字稿和页面；职级只作为决策权限语境，不直接决定结论权重。全部 VL 页面进入“有讨论/仅展示”附录（commit `829fa8d`）。
- 增加 `minutes.evidence.json`、版本化 RAG JSONL 和无需服务/LLM 的 `.meetingpack.zip` 静态查看器；Web 以轻量“依据”链接展示来源（commit `829fa8d`）。
- 修复旧录音缺少会议内 `audio.wav` 和视频 inbox 源路径消失导致的播放失败；新导入媒体固化到会议目录，同盘优先硬链接、跨盘复制，旧会议保留 `source.json` 回退（commit `7a9829f`）。
- 增加会议内“质量验收”工作区：标题栏提供显式入口，按 evidence claim 逐条标记正确或错误类型，支持原文/播放依据跳转、待判断/问题/正确筛选和 1–7 快捷键；本地追加式评测事件不改正式纪要、不复制 claim 正文，相关来源变化后判断自动失效。首页壳禁止缓存，避免服务升级后浏览器长期停留在旧入口（commits `e15f3ba`, `bfcd603`, `e46c185`；显式入口补丁回归 80/80）。
- 增加上下文感知的逐字稿中文阅读：原文/中文/双语切换、单轮原文展开、纪要依据跳转时自动双语展开相邻上下文、后台串行翻译和独立 revision sidecar。完整轮次按批次渐进显示，带进度条、停止/继续、失败续跑、滚动锚定，以及依据/当前播放轮次动态优先；批次顺序变化不改变原始语境。翻译使用页面、人员与关联结论消歧，但结论明确为低信任背景，不能向原话补事实（commits `2b46c70`, `8e829ec`；合成 Web 回归 85/85，另有优先/部分取消/续跑单元回归）。
- 会议助手接入 `meeting-rag/evidence-hybrid-v1`：统一检索当前会议的 claim、逐字稿、VL 页面和纪要章节，命中二次归纳时补回原始逐字稿，统一 `R` 引用可点击回到播放位置；增加不调用模型的检索检查接口。旧会议没有 claim linkage 时显式标记 `partial`，不把纪要章节伪装成证据（合成 Web 回归 86/86，纯虚构内容的本机真实 LLM 协议测试通过）。
- MeetingPack 升级为 `meetingpack/v2`：离线 Viewer 常驻完整逐字稿、播放器和时间轴，任意时间码可跳转；增加管理层/执行层 × 快速/精细四种确定性视图、会议理解图、右侧证据核对和跨结论/原文/页面搜索。旧会议证据不足时显式标记 `partial`，导出始终保留完整逐字稿且不反写会议 sidecar（合成 Web 回归 88/88，实际音频包完成浏览器视觉检查）。
- 会议 RAG 完成多语言 embedding + reranker：Qwen3 0.6B 双模型以独立 loopback user service 常驻，词法/稠密两路经 RRF 融合后重排，失败自动降级；向量索引按会议 revision 持久化且 manifest 不复制正文。6 场现有会议共 2545 条记录预热成功，中英双向合成检索和本地助手协议测试通过。
- MiniMax H3/ComfyUI 试跑导致本机内存压力后，停止 H3 图像服务并恢复默认文本模型组合；检索小模型增加单并发、4K context、关闭额外 cache 及 systemd 3G/5G cgroup 限制。预热后两服务约占 1.6G + 1.0G，未再次出现失控占用。
- 完成部署界面 UX 走查与 GitHub 同类项目评估：确认当前证据核对、可逆编辑和离线 Viewer 是差异化主线，同时识别刷新不恢复会议、状态语义不清、固定媒体/分栏压缩阅读区、作业阶段缺失和知识停留在单会议等问题。目标工程路线确定为“抽共享 domain core → 版本化 FastAPI routers → 渐进迁移 Vue 3/TypeScript”，暂不整体重写为 Tauri；同类功能与许可证边界记录在 `docs/UX_REVIEW_AND_REFERENCES.md`。
- 完成阅读体验 P0：启动自动恢复上次/最近会议，按 transcript/minutes revision 保存轮次与标题锚点、语言和分栏比例；视频默认紧凑，时间线区分播放进度与页面/章节，分栏支持鼠标/键盘调整。标题区增加资料/证据/分享状态；1024px 及以下会议库进入抽屉，手机使用上下阅读布局（本次提交）。
- 将底部全宽助手重构为右侧“AI 对话 / 证据”共用智能栏：宽桌面停靠、窄桌面覆盖、手机全宽；逐字稿引用与来源点击自动切换上下文，并按 evidence 是否存在提供确定性建议问题（本次提交）。
- 导出入口合并为只读预检向导，服务端只返回 evidence linkage 数量、内容数量、媒体可用性与预计体积，不泄露正文/路径；作业日志读取改为逐行受控元数据并投影中文阶段，翻译继续显示完成量（本次提交；隔离 Web 回归 89/89）。
- 增加内置 `/product` 产品介绍页，并从会议工作台和人员设置页双向进入。叙事首先解释会议机共用账号导致的人员名称不可区分，再把声纹试听、稳定人员 ID、国际化姓名与可编辑 Org Chart 组合为 Meeting Identity Core；后续分层介绍 Evidence/Knowledge Core、用户旅程、UX、技术架构、可信边界和路线图。页面零外链、零真实数据，支持桌面与手机响应式展示（本次提交）。
- 修复纪要待办和多语言逐字稿的阅读可靠性：新 Prompt 使用独立待办标题与固定表头，渲染端确定性修复旧纪要中紧贴列表项/全角竖线的表格，`minutes.evidence.json` 增加带 claim/T/P linkage 的结构化 `actions`；逐字稿改为“原文 / 译文 / 对照 + 中文/English 目标语言”，中英译文使用独立 sidecar；说话人姓名仅做定宽省略，完整国际化姓名不被截断。隔离 Web 回归 92/92，另有旧表格/行动项和中英翻译单元回归（本次提交）。
- 在线三层阅读使用“会议脉络 / 会议纪要 / 屏幕内容 / 结论审计”：双层时间线上层表示可跨时间复用的 Topic，下层表示页面每次实际出现的 Segment；同一逻辑页多次返回会生成独立 Segment，但复用一份 VL 解读。新增 `meeting-topic-map/v1` 两阶段语义归纳，把整场会议组织为 3–8 个一级论点和类型化子节点，并强制绑定 T/P/C evidence；不再把页面或几十个视觉片段当脉络节点。合格 Topic Map 默认成为首屏且只展开一级论点；右侧节点点击建立共享 Focus、不 seek，时间轴与显式范围按钮才播放，并联动屏幕、逐字稿和结论。页面变化检测默认排除右侧 15% 参会人栏；真实会议只读对比将 57 个逻辑页面中的 8 组参会人变化合并为 49 个内容页面。生成层与展示层都清洗 `<think>/<analysis>`，结论审计沿用稳定事件 ID。
- MeetingPack v4 将顶层收敛为 Viewer/README/assets，并同步在线“会议脉络 / 会议纪要 / 屏幕内容”、完整逐字稿、屏幕缩略图/VL 详情和证据跳转；合格脉络默认打开，不合格时回退纪要，不再让收件人在管理层/执行层与快速/精细四种重排视图中选择。无视频包把冻结截图作为随音频/时间切换的内容舞台，并复用在线端的 Focus 语义。导出前统一清洗 VL 的 `<think>/<analysis>` 并生成稳定页面标题，清洗结果同时进入 evidence、RAG 与 Viewer。页面导出为 960px WebP，音频为 AAC 40kbps，视频为 H.264 720p/10fps 分享版，项目母版不被修改。当前 Gate B v4 实测为无媒体 2.36MB、音频 23.43MB（原 PCM 149.55MB）、视频 40.10MB（原视频 206.92MB）。导入固化从硬链接升级为独立 inode 的 CoW reflink（失败回退复制），成功上传自动清理 inbox；新增 `meeting-storage/v1` 与白名单智能清理，只回收 PCM、VL full 帧和 RAG 索引。
- 带录屏管线改为渐进式纪要：转写/说话人完成后立即发布语音草稿，VL 完成后原位升级多模态终稿，由 `meeting-generation/v1` 记录阶段/revision/补充数量。前端自动打开草稿并保持标题阅读锚点；草稿允许播放、翻译和追问，但服务端禁止编辑、结论审计、Topic Map、重生成和导出。页面判定进一步抑制稀疏红框/激光点并增强顶部大标题；合成回归覆盖参会人栏、标注和同表格换标题。当前 Gate B 录屏旁路验证为 4674 帧 / 87 稳定段 / 51 逻辑页 / 11 摄像头段，未覆盖 canonical 资产。
- 修复长录屏会议处理中的渐进发布失效：语音草稿使用的旧 `summarize.py` 未关闭 Qwen thinking，8192 token 全部进入隐藏推理后正文为空并返回 `rc=3`，导致界面只能等 VL 终稿。现在请求显式 `enable_thinking=false`，错误行进入受控作业日志，并新增独立请求回归；会议列表、状态条和纪要区按 `has_minutes + voice_draft_rc` 区分“草稿可读”与“草稿失败，生成终稿”。正在运行的真实 VL 作业不并发补写同一会议的 minutes/evidence，避免 revision 竞态。
- 将不可重排的单 worker FIFO 改为可重排的串行优先队列：会议处理默认早于后台翻译，等待项带实际位置，用户可点“优先”排到当前运行任务之后；运行中任务不抢占。真实队列中的新会议已通过“取消并续排已有 3/367 翻译进度”即时插队，后续由正式调度器处理；独立并发单测与隔离 Web 回归覆盖类型优先、手动置顶、取消和串行执行。

## 部分完成

- 纪要版本：助手修改已有 `.history/minutes/` 与一步撤销，重新生成保留一份 `minutes.prev.md`；尚无统一版本清单、任意版本比较/恢复和 evidence 配套版本。
- RAG：单会议 embedding/reranker、revision 索引、降级和检索审计已完成；尚未建立跨会议查询、多人鉴权和会议级权限边界。
- MeetingPack Viewer：离线阅读、媒体跳转、完整逐字稿、证据状态，以及会议纪要优先的三层阅读结构已完成；旧会议不会凭空生成 claim，若要得到完整结论视图仍需用当前 prompt 重新生成 evidence。尚未完成真实收件人的可用性试验、打印/PDF 样式和无障碍专项检查。
- Schema：Prompt、evidence、RAG 与 MeetingPack 已有显式版本；Web `/api/...` 仍未版本化。
- 人工验收集：单会议标注界面、`meeting-minutes-evaluation/v1` 本地事件格式、问题分类、汇总和失效规则已完成；尚未由用户完成首轮真实会议验收，也未实现跨会议、模型版本与 prompt 版本的对比汇总。
- 翻译：逐字稿中英目标语言、原文/译文/对照阅读与整场上下文缓存已完成；尚未经过真实中文转英文、英文转中文和中英混合会议的用户旅程验证，也未实现公司/会议术语表、单轮人工改译、选择性重译和多语言纪要版本。
- 服务部署：user systemd 重启通常可用。旧 Uvicorn 曾因未关闭的 HTTP 连接在 `active_jobs=0` 下等待退出超过 45 秒；现已给优雅退出加有界超时（应用层 `MEETING_WEB_GRACEFUL_SHUTDOWN` 默认 8 秒强制关闭剩余连接，systemd 示例 `TimeoutStopSec=15` 兜底），用挂起连接的实测验证退出耗时收敛到约 8 秒。

## 仍需推进

### P1

- 给所有既有纪要生成路径统一接入多版本历史和版本浏览，并让 minutes/evidence 成套比较与恢复。
- 用 3–5 场代表性会议完成首轮真实人工验收，汇总引用准确性、决定/提议误判、虚构负责人/期限和无证据内容，再决定 prompt/model 调整；不得直接用标签自动改写正式纪要。

### P2

- 助手支持 SSE 流式响应、停止生成、稳定来源编号和“检索/核对/组织回答”阶段状态。
- 用真实会议验证 Chapter 边界和重复页面返回场景，继续校准 Topic/思维导图的数据模型；在线与离线统一“纪要优先、脉络理解、屏幕核查”的信息架构（2026-08-13 进展：两端时间轴统一为“Topic 全覆盖车道 + 说话人节奏条 + 人物 Focus”，Topic Map 升 v2 全场覆盖，真实 PLM 会议覆盖率 46% → 98.9%；剩余为提取内容质量本身与真实浏览器目测）。会议库增加重复候选、回收站、项目/标签和待核对筛选。
- Web 服务增加有界优雅退出；区分普通浏览连接、同步 LLM 请求和后台作业，部署时明确显示等待对象并在超时后安全退出。
- 支持纪要选区作为明确修改目标，以及逐字稿人工修订和“纪要已过期”状态。
- 根据真实试用反馈补充翻译术语表、语气/术语错误反馈和单轮人工改译；在确认阅读交互前不扩展到多语言纪要重写。
- 在现有单会议混合 RAG 上设计跨会议检索范围、身份鉴权、会议级 ACL 和索引删除/撤销语义；在这些边界完成前不提供全局搜索入口。
- 为删除会议提供回收站/恢复流程；当前仍是确认后直接删除。

### P3

- 渐进迁移详情前端到 Vue 3 + TypeScript + Vite；引入组件/状态边界和 Playwright 用户旅程测试，MeetingPack Viewer 保持静态单文件。
- 完整键盘导航与无障碍检查；响应式基础已覆盖 1600×900、1366×768、1024×768 和 390×844，但仍需用自动化用户旅程持续回归。
- 作业持久队列与服务重启恢复。
- 完成 Web API schema 版本化，并增加更细的性能/资源指标。
- 在 ACL 和删除/撤销语义完成后再做 typed decision/action/risk、跨会议 Inquire 和只读 MCP；不先开放无权限边界的全局搜索。

## 下一次评审触发点

按当前顺序，在以下任一节点完成后更新本文：前端 Vue/TS 迁移开始、逐字稿翻译真实试用反馈、首轮真实验收与问题汇总、统一纪要版本浏览、逐字稿人工修订与纪要过期状态、会议回收站、本地跨会议 RAG。若期间发生真实环境数据或播放事故，应先记录事故原因、恢复方式和新增回归，再重新评估优先级。
