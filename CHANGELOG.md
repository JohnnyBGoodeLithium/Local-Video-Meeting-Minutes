# 完整变更日志

本文件是面向产品、评审和交付的阅读索引。完整、可恢复的逐行代码历史以 Git 为准：

```bash
git log --date=iso --stat
git show <commit>
```

真实会议、录音、人员、声纹、组织架构、评测和作业运行数据不属于代码日志，也不得进入 GitHub。

## 未发布

## v0.9.1 — 2026-08-19

- 修复“总体结构/关键结论/待办 + 按人或项目明细”重组请求被误报为重复证据：同一事实现在可在不同阅读视角有限复用，未知 marker 仍严格拒绝，超过八倍库存数量的循环退化仍拦截。提示协议进一步禁止把用户的结构要求复制成纪要正文；没有事实支持的栏目必须省略，不能用“暂无”或复述要求填空。默认 Qwen 模型已用同类虚构请求通过真实协议测试。产品版本升至 `v0.9.1`，在线构建号升至 `20260819p62`。
- AI 对话改为“自然语言命令 + 可逆写入”：局部修改通过事实、待办语义和 revision 校验后直接同步到标准纪要并保留一步撤销；整篇重组不再覆盖 `minutes.md`，而是保存为 revision 绑定的 `minutes.views.json` AI 阅读视图，标题栏可随时在“标准纪要 / AI 纪要视图”之间切换。刷新或服务重启后仍可用“恢复上一版”从 `.history/minutes/` 恢复，且恢复前再次备份当前版本。旧客户端误把 document proposal 发往章节写入接口时服务端会拒绝。修改卡默认只显示结果摘要，完整版本按需展开；服务端安全渲染助手 Markdown 与表格，内部 marker 不再暴露。标准纪要和 AI 视图统一从结构化 action 真源重建待办表，兼容“1. 待办事项 (Action Items)”等编号/双语标题，并修复整行塞入事项字段、其余字段整体错位的存量 sidecar。普通流式问答识别 `finish_reason=length`，不会把截断内容伪装成完整回答。在线构建号升至 `20260819p66`。

## v0.9.0 — 2026-08-19

- 在线工作台新增整篇“重组纪要”：用户可用任意自然语言指定栏目、顺序和阅读重点，模型只能从独立 `meeting-facts/v1` 事实库存中选择并重排，必须逐条保留既有 evidence marker；服务端拒绝无依据正文、未知 marker、重复证据和把普通过程记录升级成正式待办。结果先预览，再应用或撤销，写入仍经过 revision 乐观锁和历史快照。会议脉络继续保持时间线性，不随纪要版式变化。
- 纪要生成同步固化 `meeting.facts.json`，其有效性只绑定逐字稿、页面时间线和 VL 描述，不绑定当前纪要版式。自然语言重组只刷新当前 `minutes.evidence.json`，不覆盖完整事实层；在线 RAG 与 MeetingPack RAG 会补入当前阅读纪要省略的 `fact` 记录，避免换结构后问答“忘记”未展示事实。
- MeetingPack 继续使用兼容的 `meetingpack/v5`，新增 `assets/facts.json` 和 manifest 事实统计；包内层级仍为 `viewer.html + README.txt + AGENTS.md + assets/`。产品版本升至 `v0.9.0`，在线构建号升至 `20260819p61`。

- Web 失败作业不再只留下红色状态：新增 `job-recovery/v1` 恢复计划，卡片说明安全分类、已保留资产和建议动作。翻译、会议脉络、本地重转写以及已形成逐字稿/VL 缓存的后半段上传或纪要任务可以只重跑对应阶段；ASR 前或视频抽帧检查点不足时明确要求重新导入，不直接重放作业 JSON 中的旧命令。恢复成功后旧红卡自动退出最近失败列表，重复点击返回 409。高质量恢复仅在管理员显式配置 refinement 模型时出现，默认恢复不自动加载大模型。在线构建号升至 `20260818p60`。
- MeetingPack Viewer 将播放器、双时间轴、说话人选择及上一段/重播/下一段统一为所有 Tab 共用的左侧工作区，右侧只显示当前选择的脉络、纪要、逐字稿或屏幕内容。进入逐字稿不再产生不可逆的三栏常驻状态；切回任一内容 Tab 会立即收起逐字稿，同时保留播放器。右侧内容区扩大，Topic Map 可按宽度自然展开多列。逐字稿普通行恢复中性白底，只在姓名标签使用稳定人物色，当前核听句才显示很浅的对应色。人物车道仍为 16px，议题/人物调色板继续分离，播放跟随仍只在核听段落变化时居中。在线工作台布局不变；现有 MeetingPack 需重新导出。
- 后台作业日志增加统一的 traceback 安全边界：对未以元数据协议开头的子进程输出仍不落盘，但若识别到 traceback 末行，只保留 `ValueError/URLError/...` 异常类名，丢弃可能包含会议正文、本地路径或请求细节的错误消息。修复“主流程只剩 rc=1、无法判断失败层”的可诊断性缺口，并增加独立隐私回归测试。
- 修复 VL 屏幕标题出现字面量 `\\n## 标题`：人读清洗现在还原 JSON-string 形态的换行，并把被压到同一行的标题/页面角色/信息价值/页面内容协议恢复为 Markdown 层级；在线前端另加旧后端容错，可从原始描述重新提取标题并隐藏重复协议段。存量 `page_desc.json` 无需重跑 VL，新导出的 MeetingPack 同步使用清洗结果。语音草稿失败提示同时按返回码区分模型请求、空正文和内部异常；未预期异常只记录脱敏异常类型，不再被日志过滤后只剩 `rc=1`。在线构建号升至 `20260818p58`。
- 修复会议库排序上线后左侧列表空白：列表元信息渲染引用了未在函数内声明的 `order`，接口数据仍在但浏览器抛出 `ReferenceError` 后停止绘制。现在从工作台排序状态显式取得该值，并把静态构建号升至 `20260818p56`；会议数据未发生删除或改写。
- 会议库默认按「最近导入」排序，并可切换会议时间/最近更新；选择持久化到本地工作台。新会议成功后在 `meta.json` 固化 `imported_at/updated_at`，旧会议优先用历史上传作业、其次用派生文件时间保守回退，不把源媒体保留的 mtime 误当导入时间。导入视频 + VTT/DOCX 时可明确选「忽略附带逐字稿」走本地 ASR；已有外部逐字稿视频会议可在「更多」中发起同样的重转写。原始 VTT/DOCX 和视频母版保留，覆盖派生资产前写入 `.versions/` 快照，失败自动恢复；复用屏幕逻辑页和 VL 缓存，不重跑视觉模型。在线构建号升至 `20260818p55`。
- MeetingPack Viewer 调整为“浏览 / 核听”两态：会议脉络、会议纪要和屏幕内容使用全宽浏览布局；逐字稿成为独立入口，进入后展开左侧画面/章节与说话人时间轴、右侧完整逐字稿及唯一一套发言回听控制。浏览过程中若媒体仍在播放，只保留紧凑悬浮播放器。顶部搜索按当前入口限定范围并独立保存关键词。在线工作台继续使用原有常驻播放器/逐字稿布局。
- 修复 MeetingPack 脉络节点点击后详情无法展开：详情模板在 `relatedPages` 初始化前引用它，造成浏览器 TDZ 运行时异常；调整初始化顺序并增加真实 Chromium 点击回归，验证节点展开留在浏览态、时间按钮才进入核听。
- 在线工作台与 MeetingPack 将长发言的可见分段升级为真实“核听段落”：上一段、重播本段、下一段和仅听此人都按分段起止时间导航，不再越过同一条长发言的剩余分段。后续分段重复显示说话人，并以“同一发言 · 2/4”替代含糊的“继续”。仅 Viewer 扩大逐字稿核听列；在线工作台窗口比例不变。在线构建号升至 `20260818p54`。

## v0.8.2 — 2026-08-18

- 会议脉络时间线恢复章节尺度：同一议题之间不超过 60 秒的短回应、过渡或未分类轮次直接归回该议题，不再把逐轮分类噪声画成几十个碎片。Teams DOCX 粗粒度重复时间戳在导航投影中按顺序切分为互斥区间，canonical 逐字稿与 evidence 不变。存量 `meeting-topic-map/v3` 读取时自动收敛，翻译层同步重用新的 canonical ranges，无需重跑 Topic Map 或 VL。构建号升至 `20260818p52`。

## v0.8.1 — 2026-08-18

- 建立独立的产品版本边界：根目录 `VERSION` 是单一真源，在线工作台、产品介绍页和 MeetingPack Viewer 显示 `v0.8.1`；健康端点、包内 README 与 manifest 记录同一版本。MeetingPack 默认文件名加入会议日期、产品版本与导出时间，避免多次导出覆盖或同名。产品版本、前端构建号、Git commit 与数据 schema 明确分离；一个产品发布可包含多个完整工程提交。构建号升至 `20260818p51`。

- 统一发言回听的“人物选择”和“播放范围”契约：选中人物但仍处于“顺次播放”时，上一段/重播/下一段继续按自然时间线工作；只有主动切到“仅听此人”才按该人物过滤并跳过其他轮次。未选人物时“仅听此人”会采用播放器当前位置的说话人，且当前位置本来就是该人的发言时不会倒回句首。新增四态身份投影：已绑定声纹可跨会议认人；VTT/DOCX 明确姓名可在本场按完全同名聚合跳播；未命名但已有 `voice_id` 的“说话人 K”可按本场声音簇跳播并继续提供绑定入口；只有片段过短、未形成可用声音簇时才禁选并显示禁止光标。导入姓名和本场声音簇都不伪造稳定 `person_id`。在线端与 MeetingPack Viewer 同步，构建号升至 `20260818p50`。

## v0.8.0 — 2026-08-18
- 在线端与 MeetingPack Viewer 新增发言级回听控制：未选择人物时可按整场执行上一段、重播本段、下一段；从说话人图例、逐人车道姓名或发言块选中人物后，可在“顺次播放”和“仅当前说话人”之间切换，后者会自动跳过其他人的轮次。当前发言会同步高亮逐字稿与个人车道。Viewer 移除说话人区域下方重复的语义摘要条，把搜索前的空间留给直接回听控制；语义 Focus 数据和议题/结论联动仍保留。构建号升至 `20260818p49`。
- Teams 导入新增官方 DOCX 逐字稿支持：网页可一次拖入一个录像与一个 `.docx`（也继续支持 `.vtt`），文件名无需相同；同时提供 VTT 与 DOCX 时明确拒绝，避免选错正文。新增零第三方依赖的 OOXML 解析边界，只接受“粗体姓名—时间码—正文”结构并校验时间顺序，忽略头像等嵌入媒体；姓名继续进入现有分离、声纹与组织身份链路，录像继续进入画面理解。会议目录保护 `source.docx` 母版，`source.json` 增加通用 transcript 字段并保留 VTT 兼容键。构建号升至 `20260818p48`。
- Topic Map 升级为 `meeting-topic-map/v3`，修复“只有少量议题覆盖、其余时间轴大片空白”的数据契约问题：局部候选获得稳定 `candidate_id`，整场归并必须声明每个候选归属；代表论据 `turn_ids/evidence_ranges` 与完整浏览 `navigation_turn_ids/ranges` 分离，顶层 `navigation_segments` 显式标注业务议题、过渡/等待和尚未分类。覆盖率改按内容轮次计算，静音时长另列；在线端和 MeetingPack 时间轴同步区分过渡斜纹与未分类琥珀块，未知内容不再按时间邻近冒充业务议题。v1/v2 存量 sidecar 继续可读。构建号升至 `20260817p47`。
- 修复纯音频会议导出中的三类可信度问题：人读纪要、证据抽屉和翻译资产不再暴露 `(T000…)` 机器主键，稳定 linkage 继续保留在隐藏标记与 sidecar；语音草稿与多模态终稿共用待办协议校验，明确承诺若被写到议题正文会触发定点修复，仍留在待办外的 `kind=action` 降为 discussion，防止“纪要称无待办、候选区却出现已确认行动”的矛盾；Topic Map 一级议题改为互斥主归属，只自动吸收不超过 3 轮/45 秒的微小空隙，长未知区间如实留空。结构化响应不再经过会删除独占花括号的 VL 人读清洗器，并增加 JSON grammar、紧凑文案上限和紧凑归并恢复，消除代码自身制造的“模型 JSON 错乱”。新增下一阶段自助 POC 工程计划与项目所有权/独立验收框架。构建号升至 `20260817p46`。
- 修复新录音“纪要看似不能跳转、会议脉络为空”：在线端与 MeetingPack 的纪要依据现在直接显示第一条原文时间码，点击一次即打开完整证据并同步跳转播放，不再要求先开依据再点第二次时间；upload/regen/topic-map 后台作业完成时，当前会议 bundle 自动刷新，脉络不会生成后仍停留空态。Topic Map 全场归并在原输出、JSON 修复和完整重试都失败时，不再整场报错丢失，而是只用已经通过局部归纳的候选主题及原 T/P/C 引用确定性组装；不凭空凑主题，结果仍经过 3–8 个一级议题质量门槛。构建号升至 `20260817p45`。
- 终稿 ready 后自动补齐另一种阅读语言：纪要、会议脉络与屏幕标题/短摘要进入最低优先级串行队列，旧会议首次打开时懒补齐；逐字稿仍手动触发，完整 VL 正文不翻译。新增 revision-bound `visuals.translation.{target}.json` 与 Web API，在线屏幕列表/Focus 舞台同步切换；MeetingPack 携带 `assets/visuals.{language}.json`，离线 Viewer 也同步切换标题和短摘要。构建号升至 `20260814p44`。
- 新增 `HANDOFF.md` 交接笔记：记录当前验证基线、进行中任务（终稿后自动翻译的完整实施方案与集成点）和遗留事项，供接手 agent/人类直接进入状态；README 文档地图与 AGENTS.md 增加指引。
- 修复屏幕内容标题大面积乱码（真实 77 分钟 Gate B 会议，69 页中约三分之一异常）：VL 模型把答案包进 LaTeX `\boxed{…}`、把 Markdown 转义成 `\## 标题`、或直接答 JSON 片段（`"标题": "…",`），清洗边界 `clean_model_text` 不处理这些形态，标题提取把 `\boxed{`、`\## 标题` 当成了页面标题，角色/信息价值正则也因引号失配退回启发式。修复：清洗层拆除 `\boxed{` 包装、还原 `\#` 等反斜杠转义、移除孤立花括号行；`visual_title` 新增 JSON 键形态标题提取并剥离候选首尾引号/逗号；`_visual_role`/`_visual_value` 的显式字段正则容忍引号包裹（冒号保持可选）。全部为读路径清洗，存量会议与导出包无需重跑 VL 即自愈。合成单测覆盖 boxed/JSON 两形态，真实 69 页验证异常标题清零。
- 修复直出模式总体纪要过短且无证据标记（真实 77 分钟 Gate B 会议）：上下文未超限时总体摘要走单次直出，该路径完全绕过 map/reduce 的退化检测、必需章节校验和待办证据标记合规护栏——模型产出总体章节零 marker（依据链接与正式待办整表为空）、阅读版投影仅两千余字。新增 `minutes_overview.generate_direct`：直出与 map/reduce 共用 `_complete_with_guard`（缺"总体摘要/待办事项"或待办行缺 `kind=action`+`turns=` 标记则 `repeat_penalty=1.2` 重试）与待办定点修复轮（`notes` 传完整结构化上下文供引用真实 T 编号）；`_repair_todo` 抽为两路共用并合并修复轮 token 计量。`minutes_by_page` 直出分支经模块级 `overview_direct` 接缝调用（测试可替换，minutes_policy_test 已切换，避免测试打真实 LLM）。合成单元回归覆盖直出合规一次过/修复拼接/修复失败保留原稿/缺章节重试四路径。
- 包内 AGENTS.md 升级为 agent 任务菜谱：从"文件地图 + 引用规则"扩展为五类场景指引——单场深读/重新摘要、**同系列多场对比**（用 `sources.transcript` 中跨包恒定的 `person_id` 对人、topic-map 标题对议题、actions 按负责人+事项语义对待办，输出标注新增/延续/翻案/消失并同时引用两场 C 编号）、会后产出（措辞服从 claim 状态分级，提议不得写成决定）、知识库索引（records.jsonl 消费规则与 meeting_id/artifact_id 版本语义）、事实核对（claim→T/P 回溯，display_only≠被决定）。文件地图补充 person_id 跨会恒定说明与 manifest 标题/日期入口；回答规则新增"措辞服从 status"一条。冒烟新增菜谱关键内容断言。定位：会后价值生产（对比、产出、建库）交给 agent + pack，应用内助手专注修正闭环。
- 修复正文"依据"链接整体消失（真实 PLM 会议）：这次重生成里模型把证据标记包进了反引号（`` `<!-- mm:evidence … -->` ``），`markdown_with_evidence_links` 只替换注释本身、留下反引号，markdown-it 把 `[依据](#mm-…)` 渲染成行内代码——前端 `a[href^="#mm-"]` 选择器匹配不到任何链接，点击跳转整体失效（待办表格因走 `_grounded_actions_table` 直写链接不受影响）。修复：替换正则兼容反引号包裹形态并连带剥掉。Web 端与 MeetingPack 导出共用此函数，一并愈合；存量会议无需重生成。合成单元回归覆盖反引号形态与旧形态，真实 PLM 纪要验证 32 条链接全部渲染为锚点、零残留。
- 修复待办表格整行渲染错乱（真实 2 小时 PLM 会议）：模型的待办表格把 `mm:evidence` 标记独占在状态列（`| 事项 | 负责人 | 期限 | <!-- marker --> |`），`_action_fields` 剥离标记后全表过滤空单元格，4 列塌成 3 列、落入"纯文本"分支——事项格残留原始竖线、负责人/期限全部显示默认"待确认"，只有状态列（来自 claim_status 映射）正确。修复：只丢弃尾部空单元格，3 单元格行按"事项/负责人/期限"拆分、状态交 claim_status 兜底；`action_items_from_claims` 对旧 sidecar 中已误判的 claim（owner 为空且 text 含竖线）用原文重拆，**无需重新生成纪要即生效**。合成单元回归覆盖新解析与旧 sidecar 兼容两路径，并用真实 PLM evidence.json 验证 7 条待办全部正确拆列。
- 修复大屏（≥1500px）打开智能栏后全页滚动链断裂：智能栏入流并排时 `.content-shell` 未声明行模板（auto 行按内容定高），`overflow:visible` 的智能栏把共享网格行顶到内容全高（实测 2317px），`.review-grid`/纪要/脉络/AI 对话全部跟着内容生长、滚动条集体消失、AI 输入框被顶出视口。修复：`.content-shell` 加 `grid-template-rows: minmax(0, 1fr)` 行约束，`.utility-panel` 加 `min-height: 0`。以用户真实环境（2480×1287 dpr1.5、面板打开、30 条长消息）经 ?diag=1 页面内测量验证：滚动链全层收敛，脉络与 AI 消息恢复溢出滚动。`?diag=1` 诊断浮层转为常驻排障特性。教训记入工程评审：此前探针全在 <1500px 视口跑（面板悬浮不入流），大屏入流分支从未被覆盖。
- 诊断与 AI 气泡渲染：`?meeting={slug}` 深链之外，左上角 logo 悬停现在显示当前前端构建号（从 app.js 的 v= 参数自动读取，无需维护），排查"用户看到的是哪个版本"不再靠猜；AI 对话气泡支持 `**粗体**` 渲染（esc 之后转换，无注入面），模型输出的 markdown 强调不再显示原始星号。
- MeetingPack 新增 `AGENTS.md`（包根目录）：给 AI agent 的数据使用指引——文件地图（evidence.json 查结论/待办、transcript.json 引用发言、topic-map.json 查议题时段、rag/records.jsonl 建索引、slides 页图只证展示不证决定）+ 回答规则（事实必附 T/P 依据、逐字稿优先于纪要、status 确定性分级）。整包拖进 agent 会话时不再只读 minutes.md。README 与 EXPORT_AND_RAG/ARCHITECTURE 文档同步，冒烟断言更新。
- 待办章节证据标记合规校验 + 定点修复：真实 2 小时 PLM 会议换用带退化防护的新代码重生成后，模型把 9 行待办写进了表格但**全部不带** `kind=action`+`turns=` 证据标记（标记被写进了关键结论/风险章节）——正式待办只从有依据的 action claim 投影，前端整表显示为空。现在 reduce 输出增加合规校验（待办章节有表格行就必须逐行带合规标记，"未形成明确待办"除外），不合规先随退化防护重试一次，仍不合规则发起定点修复轮：只根据片段事实笔记重写待办章节（`REPAIR_TODO_PROMPT`），合规后拼接回终稿，不合规保留原稿并告警。合成数据单元回归覆盖校验/修复/拼接全链路。
- 间距 token 缺口致全站间距归零（真实事故）：token 化批次只定义了 `--sp-4…24`，但全站仍引用 `--sp-27/28/30/38/41/42/45/48/50/54/55/56/60/70/90` 等 15 个未定义值——`var()` 无回退时整声明在 computed-value 阶段作废，padding/gap 静默变 0，表现为纪要正文贴左边框、脉络树间距错乱。已补齐全部缺失定义（变量名=像素值约定），`var(--line)` 误用改回 `var(--border)`；新增 `web/tests/design_tokens_test.py` 防线：无回退 `var()` 引用必须全部可解析、font-size 字面值清零、花括号平衡，纳入 `make check`。同批复查确认 `--btn-*/--chip-*/--input-*/--tab-*` 等组件变量均带回退值，属设计内可覆盖槽位。
- 会议脉络树重构为通栏布局：原"左侧 sticky 根节点列 + 右侧分支"在长会议下根节点小卡片垂直居中漂浮、左列大面积死黑、连接轨线 2px 半透明几乎不可见。改为根节点横条置顶（整场会议 · N 议题 · M 节点一行展示），分支卡片通栏排列，左侧 18px 内收 + 竖轨线 + 短横线连接，层级感保留且无死区。
- 智能栏覆盖时遮挡右栏滚动条：`<1500px` 视口下 AI 对话/证据栏以 `position:absolute` 覆盖内容右缘 430px，脉络/纪要面板的滚动条正好在遮挡区（用户报"进度条没了"）；现在 `1051–1499px` 区间打开智能栏时 `.review-grid` 右移出 430px 空间，与 ≥1500px 的并排行为一致。探针实测（CDP 级 DOM 度量）：`#minutes`/`.topic-map-view`/`.assistant-messages` 三者 overflow 链路均正常（sh>ch, overflow-y:auto），AI 对话滚动条在当前代码下确认存在——若仍看不到请硬刷新（Ctrl+Shift+R）加载 p38。
- 时间轴 hover 浮层被面板分隔条覆盖：`#tl-tip` 虽 `position:fixed; z-index:50`，但嵌在 `.player-box`（`z-index:3` 层叠上下文）内，而 `.pane-resizer` 是 `z-index:4` 的平级元素，按层叠上下文规则整条浮层被灰色分隔条压住。`#tl-tip` 移至 `<body>` 直属，逃逸局部层叠上下文。另支持 `?meeting={slug}` URL 直达指定会议（调试与分享链接可用）。
- 总体纪要退化失控防护：真实 2 小时 PLM 会议 reduce 阶段模型陷入"自我修正"循环（同一长句反复重述 22 次），6144 token 输出预算被烧光，排在最后的待办事项章节整体被截断、evidence 解析不到任何 action。`minutes_overview` 新增退化检测（同一 ≥40 字长行出现 ≥4 次，或"（注：/修正：/最终决定：/实际执行："类自我修正链 ≥4 处）→ 以 `repeat_penalty=1.2` 完整重试一次 → 仍退化则确定性清理（重复长行只留首现、自我修正链整行删除）后继续，不再把垃圾写进 minutes.md；reduce 输出缺少"## 总体摘要"或"### 待办事项"章节同样触发重试。map 片段归纳共用同一防护。`LocalLLMClient.complete` 新增 `repeat_penalty` 透传（llama-server OpenAI 接口原生支持）。合成文本单元回归覆盖检测/重试/清理/截断四路径。
- 处理提速与助手流式输出：①VL 逐页解读从串行改为有界并发（`describe_pages` 用 `ThreadPoolExecutor`，每页完成即原子落缓存，中断续跑语义不变），`meeting-vl` 服务配套调为 `--parallel 2 --ctx-size 32768`（每槽 16k），并发数由 `MEETING_VL_WORKERS`（默认 2）控制；2 小时实测会议中 VL 是最长阶段（24.6/52.5 分钟），双槽预期接近减半。stub 服务单元回归覆盖并发、缓存与空正文页降级。②AI 对话改 SSE 流式：新增 `POST /api/meetings/{slug}/assistant/chat/stream`，校验与检索在流开始前同步完成（冲突仍返回 409），帧序 meta(证据) → delta(逐段) → done；前端逐段渲染到气泡、完成后一次性重渲染使引用链接可点并持久化，中途失败自动撤掉空气泡；原非流式端点保留。③碎片声纹自动合并：入库流程收尾时，本会议中 ≤2 轮且未人工绑定的声纹用高于入库的 0.80 阈值再匹配库内其他声纹，命中即并入并清理单来源碎片条目（`voice_enroll.merge_fragment_voices`），减少“(声音N)”类碎片的手工清理。④新增 MeetingPack Viewer 模板的无头浏览器启动回归：上一批 token 化引入的 TDZ 运行时错误（`node --check` 查不出的那类）导致导出包整页空白，现有合成数据渲染 + Chromium 真启动断言正文渲染、控制台无未捕获异常；无 Chromium 环境自动跳过。
- MeetingPack 屏幕静帧提质：导出压缩从 960×540/q72（按小舞台缩略图设计，放大到 1500px 预览窗或 125–300% 缩放时模糊）改为长边上限 1600px、quality 80（按放大预览窗 min(1500px,96vw)×min(940px,94vh) 设计）；1280×720 源图不再被降采样，实测每页约 48KB→84KB，36 页无媒体包 2.0MB→3.2MB。导出预检的页面体积估算系数 0.42→0.55 保持估算可信。文档（EXPORT_AND_RAG/ARCHITECTURE）同步。旧包需重新导出获得新图。
- 界面修复：在线端打开 AI 对话/证据智能栏后，标题栏“更多”下拉菜单被智能栏盖住（`.more-menu-pop` z-index 20 < `.utility-panel` 25），菜单层级提到 40；MeetingPack Viewer 搜索结果区与下方逐字稿都是纯白无分界，搜索框与结果列表改铺 `--bg` 浅底色加下边线、结果项反白带边框，与逐字稿明确分层。说明：在线端逐字稿搜索（逐字稿栏标题右侧搜索框，Enter/Shift+Enter 跳转）上一批已上线，会议加载后自动可用。
- 修复 MeetingPack 导出 Viewer 打开全空白：设计 token 化时把 `SPK_COLORS`/`TOPIC_COLORS` 改为经 `_palRead()` 读 CSS 变量，但 `SPK_COLORS` 的初始化行排在其 `const _palRead` 定义之前，触发 TDZ 运行时错误（`Cannot access '_palRead' before initialization`）使整页 JS 中止——`node --check` 只查语法无法拦截，影响 token 化之后导出的所有包。定义已前移到首次使用之前；用真实 PLM 会议重导出验证：headless 控制台零错误、完整渲染。旧导出包不会自愈，需重新导出。导出文件名规范化：`{标题}_{日期}.meetingpack.zip`（原无日期，多次下载只能靠浏览器加 `(5)` 区分）。
- 声纹拆分升级为半监督重排 + 碎片声纹整体改派：用户反馈长会议不可能逐条找出所有错分轮次，且实测标记 16:22 单轮被“不能拆分全部轮次”拒绝——排查发现该轮属于只有 1 轮的同名碎片声纹（该会议 20 条声纹中有 2 条仅 1 轮，显示名与 150 轮的大声纹相同），拆分对它本就不适用。两点改进：①拆分从“只处理标记轮次”升级为半监督重排——以标记轮次（一条即可）建立新簇质心，再把该声纹本场**其余全部轮次**的嵌入与新旧质心逐一比较，离谁近归谁（`voice_enroll.reassign_by_centroids`，纯 argmax 余弦），响应新增 `reassigned` 计数；重排后原声纹若不再剩轮次则 400 拒绝。②前端识别“该声纹轮次被全部选中”的场景：填了“具名为”就直接调绑定整体改派（正是碎片声纹的正确操作），没填则明确提示“只有 N 轮，无需拆分，填姓名即可整体改派”，不再发出注定 400 的拆分请求；服务端该 400 文案同步改为可操作的指引。合成向量单元回归新增 4 例（样例带动重排/无移动/多簇/空输入）。验证：`make check` 与 120/120 隔离 Web 回归通过。
- 声纹按轮拆分的可操作性与一步具名：用户实测标记轮次后未生效，排查确认服务端从未收到拆分请求——问题出在前端最后一步的可发现性与静默失败路径。底部操作条改为醒目的橙色常驻条，文案明示“已选 N 轮 → 点右侧「确认拆分」生效”，按钮从“拆分为新说话人”改名“确认拆分”并加底色；新增“具名为”输入框（复用人员 datalist）：填入已知人员姓名则拆分后对每条新声纹自动调用绑定，一步完成“这些轮次是某人的”；留空则维持未绑定。`applySplitMarks` 增加 try/catch 与失败 toast，网络异常、无声纹分组、接口报错均明确提示且保留标记可重试，不再静默清除标记。验证：`make check` 与 120/120 隔离 Web 回归通过。
- 设计系统 token 化（P3 原子化第一阶段）：`style.css` 全部 `font-size`（181 处）/ 圆角（105 处）/ 间距（517 处）/ 字重收敛为 `:root` 自定义属性——字族 `--font-sans/--font-mono`、字号阶梯 `--fs-8…22`（变量名=像素值）、字重 `--fw-400/500/600`、间距 `--sp-4…24`、圆角 `--r-2…18`；议题与说话人调色板抽出为 `--topic-1…8` / `--speaker-1…8`，`app.js` 与 MeetingPack Viewer 的 JS 填色经 `getComputedStyle` 读同一组 token（读不到回退内置默认），改色一处生效全站。按钮/输入框/说话人 chip/视图切换 tab/弹窗增加组件级变量（`--btn-*`、`--input-*`、`--chip-*`、`--tab-*`、`--dialog-*`，均带回退值，可全局或局部覆盖）。新增 `web/static/theme.css` 主题插槽（注释模板 + 浅色主题示例，两个页面均在 style.css 后引入），换字体/配色/圆角只改此文件；定制合同与边界见 `docs/DESIGN_TOKENS.md`。修复窄屏（≤820px）逐字稿说话人姓名 chip 被顶到行右：`.turn` 网格改三列、chip `justify-self:start`，姓名回到时间码旁。验证：花括号平衡与零 font-size 字面值检查、`node --check`、桌面 1600px 与 390px 截图走查、`make check` 与 120/120 隔离 Web 回归通过。
- 每场会议的 AI 对话在刷新/重开浏览器后保留：消息与最近 8 轮上下文按会议存入本浏览器 localStorage（上限 50 条），与该会议逐字稿 revision 绑定——逐字稿或身份变化（绑定/拆分/改名触发）后旧对话自动作废丢弃，避免引用错位；删除会议时同步清除。对话归属用 `assistantSlug` 跟踪，切会议的清空不会覆盖目标会议的存档。服务端重启后未写入的修改预览卡在应用时明确报失败（既有错误路径）。
- 修复智能栏 AI 对话过长无滚动条：`.assistant-thread` 是块容器且 `overflow:hidden`，内部 `.assistant-messages` 高度 auto 随内容增长被整体裁掉；thread 改为 flex 列、messages `flex:1 1 auto` 获得确定高度后 `overflow-y:auto` 生效。静态夹具截图确认滚动条出现。
- 声纹按轮拆分：两位声音相近的说话人被并入同一声纹时，点击说话人 chip 打开绑定弹窗，选“有些轮次不是 TA → 逐轮标记拆分”进入标记模式，逐轮点击标记（可撤销），底部操作条确认“拆分为新说话人”。服务端 `POST /api/meetings/{slug}/split` 只对所选轮次音频段重提嵌入（`voice_enroll.embed_ranges`，整段窗口、短段补齐），贪心余弦聚类（0.70，所选轮次可能不止一个人），逐簇匹配库内其他声纹（排除来源声纹，防止又匹配回自己）或匿名新建待绑定，只改派所选轮次；原声纹质心用剩余轮次自动重算。空轮次、跨声纹、覆盖该声纹全部轮次均 400 拒绝且在校验阶段返回、不加载模型。合成向量聚类单元回归 + 冒烟负路径断言覆盖。
- 在线端阅读工作台：会议名称可在标题旁“✎ 改名”就地修改（写入会议目录 `meta.json`，服务端 `POST /api/meetings/{slug}/rename` 原子写入、1–80 字校验、乐观更新失败回滚；名称参与翻译语境 revision，改名后既有译文按过期处理）。逐字稿工具条新增搜索框：大小写不敏感包含匹配、命中整轮高亮、Enter / Shift+Enter 前后跳转，切换会议时清空。选中的一级议题把分支详情内联显示在该议题正下方（议题色左边框），不再沉到脉络图底部。窄屏（≤820px）播放证据栏改为纵向弹性布局，逐字稿获得约四成保底高度。修复真实媒体时长小于会议时长时（如 10 秒夹具音频）`loadedmetadata` 把整条时间轴重建为媒体时长、议题块全部挤出屏外的问题：时间轴时长取媒体与会议时长的较大者。
- MeetingPack Viewer 批量修复：三栏宽度重排为 430–500 / 480–860 / 240–300px，逐字稿与时间轴不再被挤碎；无视频屏幕舞台高度 190→320px，接近“看录屏”而非小方块；修复搜索结果区被 flex 压缩到只剩几像素（`flex: 0 0 auto` + 逐字稿 `min-height: 0`）；删除与播放进度重复的独立聚焦蓝条，聚焦区间叠加进 Topic 车道；修复拖动进度条被 timeupdate 回写到拖动前位置（scrubbing 期间不回写，进度范围取媒体与会议时长较大者）；窄屏修复逐字稿消失（左栏高度自适应 + 逐字稿 52vh 上限）。冒烟断言同步更新为新的聚焦区间结构。
- 录音笔音频管线补齐声纹入库：新增 `bin/voice_enroll.py`（pyannote 嵌入子模型按分离段求质心 → 与声纹库 0.70 阈值匹配，命中即跨会议具名，未命中入库待绑定，回写逐字稿并重生成试听片段），`bin/run_all.py` 在合并轮次后接入“[2.5/3] 声纹入库”（失败只警告不中断纪要）。此前录音笔导入的会议全部轮次 `voice=null`，网页端无法绑定。修复纯音频会议（无 `slides.json`）在网页端“重新生成纪要”秒败：无分页资料时改走与 run_all 相同的 `summarize.py` 整场纪要管线。

- 时间轴议题配色与术语、人物交互简化：UI 统一改称“议题”（原“论点”，English 仍为 Topic）；一级议题按序号从 8 色调色板取色（`TOPIC_COLORS`），时间轴区间块与右侧脉络“议题 NN”标签同色，替代单一蓝色。人物 chip 不再触发“人物 Focus”（原点击会滚动逐字稿、改写 Focus 摘要）：改为悬停即在主时间轴与逐人车道只显示该人发言（他人调暗、移开恢复），点击钉住、再点取消；逐人车道开关文案改为“说话人视图 ▸/▾”。MeetingPack Viewer 同步配色、术语与 hover/pin 交互。
- 时间轴密度与可读性修复：删除 Topic 车道上的结论/待办菱形刻度（结论信息保留在会议纪要中，不再占用时间轴）；逐人车道展开/收起从混在人物图例里的 chip 改为车道区块右下角的独立虚线按钮，收起时也常驻可点；说话人节奏条桶间重叠加宽并加底色，消除密集会议下的桶间白缝；Topic 车道论点块与 low_value 块提高对比度，深色面板上清晰可辨。MeetingPack Viewer 同步。
- Topic Map 生成端改为全场覆盖：局部归纳要求每个轮次归入候选论点或显式列入 `uncovered_turn_ids`，全场归并必须覆盖全部窗口材料并允许一个带 `low_value` 标记的“过渡与杂项”论点；未被挂接的轮次区间由代码按时间邻接确定性归入最近论点（只扩展 ranges/turn_ids，不改写模型文本），`stats.coverage` 记录覆盖率。schema 升 `meeting-topic-map/v2`，v1 旧图 revisions 匹配时仍判 ready。真实 2 小时 PLM 会议覆盖率从 46% 提升到 98.9%。加固：单窗归纳 JSON 修复仍失败时降级为空归纳交兜底分配（不再整场作废），归并输出 token 上限 5000→8000 且修复失败完整重试一次。
- 时间轴二轮重构：说话人泳道改为像素桶节奏条（每桶染该窗口主导人颜色，桶数与会议长度无关，不再碎片化），删除双击放大镜；人物图例改为按发言占比降序配色、显示前 6 名或占比 ≥5%、其余收进“其他(N)”，未绑定/会议机灰斜纹固定沉底并带“绑定”入口；逐人车道每人一行只画该人发言块，默认前 5–6 人，可展开其余；`low_value` 论点浅色弱化渲染。逐字稿长轮次（>90s 或 >500 字）在句子边界分块、子块点击按字符比例插值 seek。窄屏（≤820px）修复：逐字稿列表获得保底高度，不再被播放器/时间轴/图例栈压没。MeetingPack Viewer 同步节奏条、图例、逐人车道与长轮次分块。

## 2026-08-13

- `003c2f7` 回顾时间轴重构为“Topic 全覆盖车道 + 说话人泳道”：上层一级论点区间铺满整场，未覆盖的寒暄/等待时间用灰色间隙块表示且点击只跳转；下层相邻同说话人轮次合并成色块，按首次发言顺序确定性取色，悬停显示该位置发言构成，双击展开 ±60 秒、按真实轮次分辨率的区域放大镜（可点 seek，可关闭或按 Esc）；原按页面切换的片段层移除。时间轴下方新增人物图例（色点 + 姓名 + 发言占比），点击进入人物 Focus（高亮而非过滤）：泳道其他人降透明度、逐字稿该人轮次高亮、摘要显示发言轮数与相关结论，未绑定声纹的人在图例中提示去逐字稿绑定；逐字稿说话人 chip 以同色左边框弱关联。逐字稿整轮点击即定位播放（选中文字时不触发）；820px 及以下轮次改为两行布局、逐字稿工具条允许折行；逐字稿“译为”目标语言默认值跟随界面语言，用户手动选择后不再被覆盖。离线 MeetingPack Viewer 同步双车道与人物图例，Focus 语义与在线端一致。
- `8321f2a` 纯结构重构：`web/server.py`（约 1900 行）拆分为 `deps.py`（路径/锁/共享助手）、`job_store.py`（作业状态与管线 runner）和 `routers/` 下 10 个按域划分的路由模块；`server.py` 收敛为约 55 行装配入口。所有路由路径、请求/响应 schema 与处理逻辑逐字节不变。
- `80d1764` 修复英文脉络视图仍出现中文辅助标签：时间轴悬浮层、整场/语义 Focus、结构节点与出现区间统计、当前论点提示和无视频屏幕舞台现均随阅读语言切换；MeetingPack 同步。
- `8bc559b` 补齐会议脉络双语阅读：Topic Map 的全场摘要、一级论点与子节点使用同构 sidecar 翻译，节点 ID、类型、时间范围及 T/P/C linkage 永远取 canonical；在线脉络 UI 全量本地化，MeetingPack 同步携带导出前已生成的脉络语言版本。
- `ab16750` 新增“中文 / EN”阅读语言开关：在线界面与会议纪要同步切换；纪要译文采用不覆盖原文、revision-bound、保留 evidence marker 的分块 sidecar。MeetingPack 升级 v5，可离线携带并切换导出前已生成的双语纪要。服务中断后的 `visual_enrichment` 可在确认无活动 writer 时复用 transcript、slides 与 VL cache 阶段级续跑，不再要求重新上传或从头理解全部页面。
- `90d99c4` 正式待办与模型 action claim 分层：只有整场待办章节、非 informational 且有逐字稿依据的 action 进入 Web、MeetingPack、章节行动组和 RAG 行动加权；逐页误标保留为证据但不再污染待办。结论审计默认显示重点结论，并可切换查看全部证据。

## 2026-08-12

- `bc64f01` 在线工作台和 MeetingPack 的当前屏幕增加沉浸式放大、125%–300% 缩放、双击放大及前后逻辑页导航；切页更新播放时间和共享 Focus，但不自动开始播放。新增硬件适配层和无 GPU 回归：PyTorch 自动区分 CUDA/ROCm/CPU，旧 NVIDIA 自动从 BF16 回退 FP16；ASR、aligner、pyannote、文本/VL 模型路径及端口改为环境变量。README 从“钉钉闪记本地版”重写为本地多模态会议知识工作台，补充产品核心、架构、目录、数据资产和文档地图；同时新增跨机器部署/NVIDIA 验收矩阵、2 小时实测成本模型、无凭据部署模板和仓库级 `AGENTS.md`。
- `ba6ec1c` 重构在线工作台与 MeetingPack 的首屏阅读路径：通过质量门槛的 3–8 个一级论点“会议脉络”成为第一眼，纪要继续作为正式记录；脉络首屏只展开一级论点，选中后才展开当前分支。节点点击只建立语义 Focus，不改变播放时间；时间轴、逐字稿时间码和显式范围按钮才跳转播放，并持续联动高亮逐字稿、结论、时间范围和当前屏幕。无视频导出包使用冻结截图作为随音频/时间变化的内容舞台；Topic Map 缺失或不合格时安全回退会议纪要。
- `7e41911` `1bd8f51` 修复真实长会议的待办与议题阅读失真：常规纪要把带逐字稿 T ID 的结构化行动项作为“可核验待办”，原模型表中尚未关联来源的内容保留为默认收起的“待核实候选”，既不丢信息也不把建议伪装成正式任务；重复且易失控的“议题板块”Markdown 从常规纪要移到独立“会议脉络”。长会总体归纳设置结论/行动/议题数量上限并减少串行分块，Topic Map 的局部和全局 JSON 都支持只修语法的自动恢复，默认处理窗由约 8 分钟调整为 15 分钟。
- `ae285e9` 修复超长录屏会议在 VL 完成后仍生成终稿失败：总体摘要改为带 T/P 证据编号的连续分段归纳再全局合并；按页纪要继续使用原有受控分组。Web 重生成复用逐字稿、页面和有效 VL 缓存，只补缺失页面，并在成功后发布 ready 状态与刷新会议脉络。
- `63e4278` `5f88ca5` 修复屏幕内容价值误判：缺失或 reasoning 清洗后为空的 VL 说明现在显示“待解析”，不再因字数少自动降为“低信息”；只有明确的空白、过渡或会议 UI 信号才能判低。详细 VL 正文为空时降级为紧凑 JSON 视觉读取（标题/类型/摘要）并转成页面说明，仍失败才保持待解析，空正文永不进入成功缓存。
- `b2945ad` 将本机文本模型协议、上下文预算和语音草稿策略拆入独立 `meeting_core` 模块；短会议保持直接生成，超出上下文预算的长会议按稳定 T ID 连续分段，先提取事实笔记再全局合并，避免 HTTP 400 使渐进草稿缺失。在架构中预留“企业同事上传、本机队列处理、受控网页阅读与本机 LLM 对话”的身份、ACL、审计和存储边界；当前不开放网络监听。建立本文件作为人类可读索引；每一项的精确差异和验证记录继续由 Git 提交与工程走查承载。

- `a7bb83a` 增加可插队的串行优先级作业队列。
- `561a812` 保证语音草稿关闭 thinking，并明确区分空正文失败。
- `6dd94b1` 简化离线 MeetingPack 查看器。
- `fb836d7` 增加“语音草稿 → 视觉补充 → 多模态终稿”的渐进处理流程。
- `4980565` 重塑会议脉络、屏幕内容和常规纪要的阅读关系。
- `8d579d4` 优化章节结构与结论审计交互。
- `ef570b2` 增加章节和屏幕内容视图。
- `ae719b6` 优化多语言逐字稿和待办事项展示。
- `6f886ba` 修正终稿生成阶段标签。
- `eff9135` 修正窄侧栏中的作业进度可读性。
- `88a2eb2` 保证会议作业使用项目虚拟环境。
- `1abc18e` 增加产品介绍页面。
- `d9fb6fb` 优化会议回顾工作台 UX。

## 2026-08-11

- `101aec2` 记录 UX 路线与同类会议应用参考。
- `2a0b5e9` 发布混合检索、reranker 和 MeetingPack v2。
- `b102451` 增加带证据引用的单会议 RAG。
- `9a29889` 记录 Web 重启连接排空问题。
- `cd7cbd4` 记录渐进翻译 UX。
- `8e829ec` 增加逐批流式展现的逐字稿翻译。
- `7cc10b2` 记录逐字稿翻译试运行。
- `2b46c70` 增加会议上下文感知翻译。
- `abb0a41` 记录明确的质量验收入口。
- `e46c185` 明确会议结论质量验收入口。
- `bfcd603` 修复空纪要的质量状态。
- `1a82ea2` 记录纪要质量验收发布结果。
- `e15f3ba` 增加基于 evidence 的结论质量审计。
- `9cbc3d1` 将工程走查改为持续维护台账。
- `7a9829f` 修复导入会议媒体的持久回放。

## 2026-08-10

- `829fa8d` 增加证据关联 MeetingPack 和 RAG 导出。
- `cb6e6f6` 清洗组织架构模型输出。
- `ab253d3` 修复组织架构编辑后分支消失。
- `3895258` 修复删除节点时误删全部子节点。
- `4f97217` 增加组织节点就地快捷操作。
- `ceed460` 移除拖动组织节点时的确认弹窗。
- `d8e410d` 改善大型组织图缩放和布局。
- `f5d404d` 在声纹库 schema 迁移前自动备份。
- `354bf0a` 改善国际化人员身份与组织架构确认流程。
- `380e10c` 重构会议回顾工作台布局。
- `639a507` 增加本地会议助手与工程安全边界。
- `c4c10fd` 建立隔离私有数据的本地 Git 基线。
