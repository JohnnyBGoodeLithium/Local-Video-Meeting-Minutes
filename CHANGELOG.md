# 完整变更日志

本文件是面向产品、评审和交付的阅读索引。完整、可恢复的逐行代码历史以 Git 为准：

```bash
git log --date=iso --stat
git show <commit>
```

真实会议、录音、人员、声纹、组织架构、评测和作业运行数据不属于代码日志，也不得进入 GitHub。

## 未发布

- 重构在线工作台与 MeetingPack 的首屏阅读路径：通过质量门槛的 3–8 个一级论点“会议脉络”成为第一眼，纪要继续作为正式记录；脉络首屏只展开一级论点，选中后才展开当前分支。节点点击只建立语义 Focus，不改变播放时间；时间轴、逐字稿时间码和显式范围按钮才跳转播放，并持续联动高亮逐字稿、结论、时间范围和当前屏幕。无视频导出包使用冻结截图作为随音频/时间变化的内容舞台；Topic Map 缺失或不合格时安全回退会议纪要。
- 修复真实长会议的待办与议题阅读失真：常规纪要把带逐字稿 T ID 的结构化行动项作为“可核验待办”，原模型表中尚未关联来源的内容保留为默认收起的“待核实候选”，既不丢信息也不把建议伪装成正式任务；重复且易失控的“议题板块”Markdown 从常规纪要移到独立“会议脉络”。长会总体归纳设置结论/行动/议题数量上限并减少串行分块，Topic Map 的局部和全局 JSON 都支持只修语法的自动恢复，默认处理窗由约 8 分钟调整为 15 分钟。
- 修复超长录屏会议在 VL 完成后仍生成终稿失败：总体摘要改为带 T/P 证据编号的连续分段归纳再全局合并；按页纪要继续使用原有受控分组。Web 重生成复用逐字稿、页面和有效 VL 缓存，只补缺失页面，并在成功后发布 ready 状态与刷新会议脉络。
- 修复屏幕内容价值误判：缺失或 reasoning 清洗后为空的 VL 说明现在显示“待解析”，不再因字数少自动降为“低信息”；只有明确的空白、过渡或会议 UI 信号才能判低。详细 VL 正文为空时降级为紧凑 JSON 视觉读取（标题/类型/摘要）并转成页面说明，仍失败才保持待解析，空正文永不进入成功缓存。
- 将本机文本模型协议、上下文预算和语音草稿策略拆入独立 `meeting_core` 模块。
- 短会议保持直接生成；超出上下文预算的长会议按稳定 T ID 连续分段，先提取事实笔记再全局合并，避免 HTTP 400 使渐进草稿缺失。
- 在架构中预留“企业同事上传、本机队列处理、受控网页阅读与本机 LLM 对话”的身份、ACL、审计和存储边界；当前不开放网络监听。
- 建立本文件作为人类可读索引；每一项的精确差异和验证记录继续由 Git 提交与工程走查承载。

## 2026-08-12

- `a7bb83a` 增加可插队的串行优先级作业队列。
- `561a812` 保证语音草稿关闭 thinking，并明确区分空正文失败。
- `6dd94b1` 简化离线 MeetingPack 查看器。
- `fb836d7` 增加“语音草稿 → 视觉补充 → 多模态终稿”的渐进处理流程。
- `4980565` 重塑会议脉络、屏幕内容和常规纪要的阅读关系。
- `8d579d4` 优化章节结构与结论审计交互。
- `ef570b2` 增加章节和屏幕内容视图。
- `ae719b6` 优化多语言逐字稿和待办事项展示。
- `6f886ba` 修正终稿生成阶段标签。
- `eff9135` 修正窄侧栏中的作业进度可读性。
- `88a2eb2` 保证会议作业使用项目虚拟环境。
- `1abc18e` 增加产品介绍页面。
- `d9fb6fb` 优化会议回顾工作台 UX。

## 2026-08-11

- `101aec2` 记录 UX 路线与同类会议应用参考。
- `2a0b5e9` 发布混合检索、reranker 和 MeetingPack v2。
- `b102451` 增加带证据引用的单会议 RAG。
- `9a29889` 记录 Web 重启连接排空问题。
- `cd7cbd4` 记录渐进翻译 UX。
- `8e829ec` 增加逐批流式展现的逐字稿翻译。
- `7cc10b2` 记录逐字稿翻译试运行。
- `2b46c70` 增加会议上下文感知翻译。
- `abb0a41` 记录明确的质量验收入口。
- `e46c185` 明确会议结论质量验收入口。
- `bfcd603` 修复空纪要的质量状态。
- `1a82ea2` 记录纪要质量验收发布结果。
- `e15f3ba` 增加基于 evidence 的结论质量审计。
- `9cbc3d1` 将工程走查改为持续维护台账。
- `7a9829f` 修复导入会议媒体的持久回放。

## 2026-08-10

- `829fa8d` 增加证据关联 MeetingPack 和 RAG 导出。
- `cb6e6f6` 清洗组织架构模型输出。
- `ab253d3` 修复组织架构编辑后分支消失。
- `3895258` 修复删除节点时误删全部子节点。
- `4f97217` 增加组织节点就地快捷操作。
- `ceed460` 移除拖动组织节点时的确认弹窗。
- `d8e410d` 改善大型组织图缩放和布局。
- `f5d404d` 在声纹库 schema 迁移前自动备份。
- `354bf0a` 改善国际化人员身份与组织架构确认流程。
- `380e10c` 重构会议回顾工作台布局。
- `639a507` 增加本地会议助手与工程安全边界。
- `c4c10fd` 建立隔离私有数据的本地 Git 基线。
