# Changelog

本文件只记录每个正式版本对用户、稳定合同和可信验证的高信息密度摘要。

- 当前状态：[docs/STATUS.md](docs/STATUS.md)
- 功能台账：[docs/PRODUCT_FUNCTIONS.md](docs/PRODUCT_FUNCTIONS.md)
- 工程历史：[docs/history/ENGINEERING_CHANGES.md](docs/history/ENGINEERING_CHANGES.md)
- 实验记录：[docs/research/EXPERIMENT_LOG.md](docs/research/EXPERIMENT_LOG.md)
- 完整历史快照：[docs/archive/CHANGELOG_FULL_2026-08-31.md](docs/archive/CHANGELOG_FULL_2026-08-31.md)
- 完整代码历史：Git

## Unreleased

重要的未发布用户变化才写入本节；普通实现细节留在 Git。

- 新增默认关闭的 Experimental Live Context：可直接粘贴能够安全解析为公开、无 DRM 原生 HLS 的直播页面或 HLS 地址，在不可听播放、不打开浏览器的情况下持续累积暂定上下文；启动后进入独立工作区查看真实滚动文字、状态和时长，退出工作区不中断后台采集，结束后再复用现有视觉补完与 canonical 输出流程。
- 新增可恢复 `.live/` 中间层、Teams/WebVTT 来源适配、HLS 滑动列表、字幕 OCR 合并、近实时 ASR/暂定人物、资源降级和选择性画面触发；browser 静音捕获仍保持为未验证限制。
- 说话人模型改为本地优先级解析且禁止隐式下载；单独 runtime pack 只能在 license、归属、版本和子模型再分发条件全部明确时构建，应用发布包仍无权重。
- 导入设置明确区分“快速纪要”和“完整分析”；左侧队列直接显示正在处理、构建或翻译什么；会议脉络继续优先高度归并，但不再把第 9 个以后的有效议题静默截断。
- 产品介绍站收敛为“找到重点 → 核对事实 → 修正来源 → 继续使用”的七段双语旅程，加入仅使用虚构 Northstar 数据的会议／视频演示，并完成 Source Fold 响应式与无障碍视觉系统。
- 新增 GitHub Pages 静态构建与部署门禁；公开站点不访问会议、作业、人物或私有报告数据。
- 嘈杂或远场录音中，同一已确认人物不再以普通相似度连续吞并多个声音组；混声修复允许用户明确改正此前人工确认过的片段，同时继续禁止系统自动覆盖确认结果，“下一步”计算预览时即时显示处理中并阻止重复提交。
- 新导入的白板、纸面笔记和现场照片会按批次进入本地 Vision；解读进入纪要、Viewer 与知识投影，已有终稿优先复用缓存快速同步，图片本身仍不能独立证明会议决定。
- 新增默认关闭的 Experimental Companion：通过私有 Tailscale Serve HTTPS 与应用内一次性配对，让手机发送链接/小文件、查看安全进度与会议投影、回听 evidence 并复用现有人物确认和撤销。FastAPI 仍仅监听 localhost，不使用 Funnel。
- Companion 配对链接改为 URL fragment 携带一次性 token，token 不再出现在 HTTP request target 与访问日志中；手机端刷新后可通过本地 job 指针继续追踪已提交任务，revoke 后立即失效。

## v0.15.3 — 2026-09-01

- 修正正式 Application Release Bundle 的 allowlist，确保发布授权标记随制品进入全新目录验证。
- 增加发布包必须包含授权标记的回归门禁，并修复 Headless Chromium 启动端口文件的瞬时读取竞态。
- 保留失败的 `v0.15.2` tag 作为不可移动审计记录；该标签未产生 GitHub Release 或公开发布资产。
- 产品 UI、canonical schema、`job-progress/v2`、模型管线和 RAG 行为保持不变。

## v0.15.2 — 2026-09-01

- 仓库入口拆分为结构一致的完整英文与中文 README，并补充隐私安全、贡献、Pull Request 和 Issue 入口。
- 产品版本元数据统一投影自 `VERSION`，基础运行与 CI 依赖通过 Python 3.11 的 pip-tools lock 固定。
- 新增 allowlist 驱动、带 manifest 与 SHA256 的脱敏 Application Release Bundle，以及全新目录验证。
- 新增严格校验 tag、版本、变更摘要、发布说明与制品的 GitHub Release workflow，并将 `0.x` 发布标记为 pre-release。

## v0.15.1 — 2026-08-31

- 逐字稿完成多处修正后，可以一次快速同步正式纪要与 evidence；完整 VL 缓存被严格复用，不重跑 ASR、说话人或视觉分析。
- 正式纪要先发布，Topic Map、翻译和关键字随后以低优先级更新；缓存缺页时拒绝伪装成快速完成并保留标准重生成。
- “结论审计”从常驻一级页签收敛为条件式“核对关键结论”，本地评测数据与证据能力继续保留。
- 覆盖快速同步成功、缓存不足、下游排队和关键结论状态的自动化回归。

## v0.15.0 — 2026-08-31

- 用户可以直接看到当前处理阶段、已经可用的结果和可信的 ETA 范围，第一份可读结果与完整结果分开表达。
- 失败状态说明原因、影响和已保留资产，并优先从最近安全检查点继续；等待、暂停、降级和取消不再都显示成红色失败。
- “屏幕内容”更新为“画面与资料”，现场资料支持导入前缩略图、渐进定位以及导入后的查看、改名和删除。
- 发布 `job-progress/v2` 与结构化 failure/recovery 合同；旧作业继续通过明确标记的兼容投影读取。
- `make check`、隔离 smoke 与 Headless Chromium 的处理、恢复和现场资料旅程通过。

## v0.14.2 — 2026-08-31

- 人物核对完整支持中英文界面，身份确认和复杂混声修复保持相同语义。
- 恢复导出时的音频/视频选择，并修复导入入口重复符号和相关前端回归。
- 通过人物核对、导出选项和浏览器完整旅程验证。

## v0.14.1 — 2026-08-31

- 说话人身份确认改为轻量身份卡，只有用户指出“混入其他人”时才进入非阻塞高级修复。
- 混声修复默认只处理人工手选片段；系统相似建议必须主动开启，人工确认身份受到保护。
- Preview 支持多个结果组、修改前后摘要、代表片段试听和应用后就近撤销。
- 普通界面移除声纹、聚类、拆分等内部术语，并保持播放时间与逐字稿位置。

## v0.14.0 — 2026-08-28

- MeetingPack 与 AI Context 收敛为两种主要导出意图：离线人工回顾与交给通用 AI 使用。
- AI Context 保留纪要、脉络、画面文字、逐字稿、时间码和证据编号，不自动上传媒体或本机路径。
- 既有 KB Markdown/HTML 保留为高级兼容能力，知识发布继续使用 revision 和服务端凭据边界。

## v0.13.1 — 2026-08-28

- 知识发布状态以真实 provider 返回为准，不再把请求成功误认为远端文档已经可检索。
- 远端处理中、失败和可用状态被统一正规化，并保留安全回执供恢复与诊断。
- 使用虚构知识文档验证异步状态和错误投影，不把远端服务当作 canonical 真源。

## v0.13.0 — 2026-08-28

- 会议和媒体可以从工作台一键发布到管理员配置的知识目标，正文 revision 保证重复点击幂等。
- 现场照片获得受保护原图、阅读副本、可选时间定位，并同步进入 Web、Viewer、MeetingPack 与知识投影。
- `KnowledgeSink` 抽象隔离 provider 协议、凭据和删除语义；下游不得反写 canonical。
- 覆盖知识预检、发布、替换、照片安全路径与投影测试。

## v0.12.0 — 2026-08-27

- 双语产品介绍页更新为当前会议/视频上下文工作流叙事，并明确 PoC 与多人生产服务的边界。
- 工作台、Viewer 和产品页共享 Fluent 语义 token、焦点、按钮、字段、Tab 与面板基础合同。
- 产品 minor/major 版本与介绍页内容版本建立门禁，避免版本升级后继续展示过期定位。

## v0.11.1 — 2026-08-27

- WeKnora 被纳入知识交付闭环，仓库提供健康检查、低并发配置、服务模板和脱敏验收步骤。
- 单机资源守护协调会议处理与知识服务，急件任务优先且避免多个重模型同时耗尽内存。
- 知识库仍是下游消费方；凭据、数据库和真实知识内容不进入仓库。

## v0.11.0 — 2026-08-26

- 媒体模式支持受限的单条公开视频链接导入，固化本地母版并保存脱敏来源元数据。
- 会议与媒体在列表、导入和任务卡中分流；媒体使用来源标题、平台和发布者语义，并可跳回安全原链接。
- 前端开始按稳定责任域迁移为无构建 ES modules，未改变播放器与 canonical 数据合同。
- 下载失败、来源安全和媒体/会议分类获得专项回归。

## v0.10.1 — 2026-08-26

- 会议关键字、相关内容建议和多内容 ContentPack 支持用户显式选择的一组来源，仍保留每场证据边界。
- 媒体视频按镜头和论证角色组织，不再套用会议待办；在线端与 Viewer 使用自适应人物/叙事时间线。
- 新增 KB Markdown 与自包含图文 HTML 导出，时间码可使用配置后的安全 Web 深链，分析 JPEG 可直接复用。
- 长发言获得稳定时间切片，纯页面 evidence 使用真实出现时间而非 `00:00`。
- 复杂视觉页支持限量二次复核，普通页面继续使用轻量主流程。

## v0.10.0 — 2026-08-19

- ASR、术语 Context、短片复核和 provider 回退形成可移植转写修正闭环；远端回退必须显式配置。
- 逐字稿支持原文修正、快照与撤销，身份与纪要更新使用 revision 和旧结果防护。
- 说话人支持跨会声纹候选、人工绑定、保护和恢复；极短或证据不足片段保持待确认。
- 作业可以从保存的 ASR、人物或页面检查点恢复，并在安全边界内暂停让急件先处理。
- 正式待办必须有逐字稿行动证据，MeetingPack 可在处理完成前导出核听版。

## v0.9.2 — 2026-08-19

- 术语 Context 在 ASR 前注入标题、确认术语和高置信候选，不进行无声学依据的强制替换。
- 短插话利用字级时间证据保留，同时过滤孤立说话人抖动。
- 正式纪要增加草稿/终稿覆盖审计和模型分层，高质量恢复才使用更重模型。

## v0.9.1 — 2026-08-19

- 对话式纪要修改在 evidence 和 revision 校验后写入，不再直接覆盖来源不明的文本。
- 保存上一版正式纪要，用户可以在修改后恢复。
- 修改失败保留原内容和诊断状态，不生成半写入结果。

## v0.9.0 — 2026-08-19

- 会议列表按上传时间、会议时间或最近更新排序，并支持改名、搜索与深链恢复。
- 逐字稿、翻译、纪要和 evidence 使用 revision 协调过期状态，避免来源变化后继续展示旧结果。
- 独立事实层与自然语言纪要视图上线：阅读结构可以变化，canonical 纪要和事实库存不被覆盖。
- Viewer 的播放器、时间线、人物车道和核听控制在各内容页签保持一致。

## v0.8.2 — 2026-08-18

- Topic Map 将长会议归并为少量一级议题，改善时间线过碎问题。
- 脉络节点与播放器导航解耦，点击先聚焦内容而不是跳到区间末尾。
- 旧会议继续通过兼容投影读取，并完成脉络结构回归。

## v0.8.1 — 2026-08-18

- 人物选择和播放范围分离：可以高亮某人但继续顺次播放，也可以明确“仅听此人”。
- 上一段、重播本段、下一段在在线端与 Viewer 使用一致规则。
- 导出命名加入产品版本和时间，重复导出不覆盖；VTT/DOCX 明确姓名可直接导航。

## v0.8.0 — 2026-08-18

- 建立音频、录屏和 Teams 文稿的本地处理主线，生成带时间码和说话人的原语言逐字稿。
- 共享画面按逻辑页提取并由本地 VL 解读，纪要 claim 可以回到逐字稿、原声与画面。
- 发布自包含 MeetingPack Viewer，离线阅读不依赖服务、模型或 CDN。
- 支持本地身份管理、组织资料、双语阅读、单场 RAG 和证据问答的首个完整 PoC。

## 早期 PoC — 2026-08-17 及以前

- 建立从单个录音/视频到逐字稿、说话人、画面和纪要的最初本地流程。
- 用真实但不进入仓库的受控资料发现长会议、多人混声、时间码和 Viewer 可靠性问题。
- 早期逐提交细节保留在 Git 与完整历史快照，不作为当前产品合同。
