# 完整变更日志

本文件是面向产品、评审和交付的阅读索引。完整、可恢复的逐行代码历史以 Git 为准：

```bash
git log --date=iso --stat
git show <commit>
```

真实会议、录音、人员、声纹、组织架构、评测和作业运行数据不属于代码日志，也不得进入 GitHub。

## 未发布

- 在线端阅读工作台：会议名称可在标题旁“✎ 改名”就地修改（写入会议目录 `meta.json`，服务端 `POST /api/meetings/{slug}/rename` 原子写入、1–80 字校验、乐观更新失败回滚；名称参与翻译语境 revision，改名后既有译文按过期处理）。逐字稿工具条新增搜索框：大小写不敏感包含匹配、命中整轮高亮、Enter / Shift+Enter 前后跳转，切换会议时清空。选中的一级议题把分支详情内联显示在该议题正下方（议题色左边框），不再沉到脉络图底部。窄屏（≤820px）播放证据栏改为纵向弹性布局，逐字稿获得约四成保底高度。修复真实媒体时长小于会议时长时（如 10 秒夹具音频）`loadedmetadata` 把整条时间轴重建为媒体时长、议题块全部挤出屏外的问题：时间轴时长取媒体与会议时长的较大者。
- MeetingPack Viewer 批量修复：三栏宽度重排为 430–500 / 480–860 / 240–300px，逐字稿与时间轴不再被挤碎；无视频屏幕舞台高度 190→320px，接近“看录屏”而非小方块；修复搜索结果区被 flex 压缩到只剩几像素（`flex: 0 0 auto` + 逐字稿 `min-height: 0`）；删除与播放进度重复的独立聚焦蓝条，聚焦区间叠加进 Topic 车道；修复拖动进度条被 timeupdate 回写到拖动前位置（scrubbing 期间不回写，进度范围取媒体与会议时长较大者）；窄屏修复逐字稿消失（左栏高度自适应 + 逐字稿 52vh 上限）。冒烟断言同步更新为新的聚焦区间结构。
- 录音笔音频管线补齐声纹入库：新增 `bin/voice_enroll.py`（pyannote 嵌入子模型按分离段求质心 → 与声纹库 0.70 阈值匹配，命中即跨会议具名，未命中入库待绑定，回写逐字稿并重生成试听片段），`bin/run_all.py` 在合并轮次后接入“[2.5/3] 声纹入库”（失败只警告不中断纪要）。此前录音笔导入的会议全部轮次 `voice=null`，网页端无法绑定。修复纯音频会议（无 `slides.json`）在网页端“重新生成纪要”秒败：无分页资料时改走与 run_all 相同的 `summarize.py` 整场纪要管线。

- 时间轴议题配色与术语、人物交互简化：UI 统一改称“议题”（原“论点”，English 仍为 Topic）；一级议题按序号从 8 色调色板取色（`TOPIC_COLORS`），时间轴区间块与右侧脉络“议题 NN”标签同色，替代单一蓝色。人物 chip 不再触发“人物 Focus”（原点击会滚动逐字稿、改写 Focus 摘要）：改为悬停即在主时间轴与逐人车道只显示该人发言（他人调暗、移开恢复），点击钉住、再点取消；逐人车道开关文案改为“说话人视图 ▸/▾”。MeetingPack Viewer 同步配色、术语与 hover/pin 交互。
- 时间轴密度与可读性修复：删除 Topic 车道上的结论/待办菱形刻度（结论信息保留在会议纪要中，不再占用时间轴）；逐人车道展开/收起从混在人物图例里的 chip 改为车道区块右下角的独立虚线按钮，收起时也常驻可点；说话人节奏条桶间重叠加宽并加底色，消除密集会议下的桶间白缝；Topic 车道论点块与 low_value 块提高对比度，深色面板上清晰可辨。MeetingPack Viewer 同步。
- Topic Map 生成端改为全场覆盖：局部归纳要求每个轮次归入候选论点或显式列入 `uncovered_turn_ids`，全场归并必须覆盖全部窗口材料并允许一个带 `low_value` 标记的“过渡与杂项”论点；未被挂接的轮次区间由代码按时间邻接确定性归入最近论点（只扩展 ranges/turn_ids，不改写模型文本），`stats.coverage` 记录覆盖率。schema 升 `meeting-topic-map/v2`，v1 旧图 revisions 匹配时仍判 ready。真实 2 小时 PLM 会议覆盖率从 46% 提升到 98.9%。加固：单窗归纳 JSON 修复仍失败时降级为空归纳交兜底分配（不再整场作废），归并输出 token 上限 5000→8000 且修复失败完整重试一次。
- 时间轴二轮重构：说话人泳道改为像素桶节奏条（每桶染该窗口主导人颜色，桶数与会议长度无关，不再碎片化），删除双击放大镜；人物图例改为按发言占比降序配色、显示前 6 名或占比 ≥5%、其余收进“其他(N)”，未绑定/会议机灰斜纹固定沉底并带“绑定”入口；逐人车道每人一行只画该人发言块，默认前 5–6 人，可展开其余；`low_value` 论点浅色弱化渲染。逐字稿长轮次（>90s 或 >500 字）在句子边界分块、子块点击按字符比例插值 seek。窄屏（≤820px）修复：逐字稿列表获得保底高度，不再被播放器/时间轴/图例栈压没。MeetingPack Viewer 同步节奏条、图例、逐人车道与长轮次分块。

## 2026-08-13

- `003c2f7` 回顾时间轴重构为“Topic 全覆盖车道 + 说话人泳道”：上层一级论点区间铺满整场，未覆盖的寒暄/等待时间用灰色间隙块表示且点击只跳转；下层相邻同说话人轮次合并成色块，按首次发言顺序确定性取色，悬停显示该位置发言构成，双击展开 ±60 秒、按真实轮次分辨率的区域放大镜（可点 seek，可关闭或按 Esc）；原按页面切换的片段层移除。时间轴下方新增人物图例（色点 + 姓名 + 发言占比），点击进入人物 Focus（高亮而非过滤）：泳道其他人降透明度、逐字稿该人轮次高亮、摘要显示发言轮数与相关结论，未绑定声纹的人在图例中提示去逐字稿绑定；逐字稿说话人 chip 以同色左边框弱关联。逐字稿整轮点击即定位播放（选中文字时不触发）；820px 及以下轮次改为两行布局、逐字稿工具条允许折行；逐字稿“译为”目标语言默认值跟随界面语言，用户手动选择后不再被覆盖。离线 MeetingPack Viewer 同步双车道与人物图例，Focus 语义与在线端一致。
- `8321f2a` 纯结构重构：`web/server.py`（约 1900 行）拆分为 `deps.py`（路径/锁/共享助手）、`job_store.py`（作业状态与管线 runner）和 `routers/` 下 10 个按域划分的路由模块；`server.py` 收敛为约 55 行装配入口。所有路由路径、请求/响应 schema 与处理逻辑逐字节不变。
- `80d1764` 修复英文脉络视图仍出现中文辅助标签：时间轴悬浮层、整场/语义 Focus、结构节点与出现区间统计、当前论点提示和无视频屏幕舞台现均随阅读语言切换；MeetingPack 同步。
- `8bc559b` 补齐会议脉络双语阅读：Topic Map 的全场摘要、一级论点与子节点使用同构 sidecar 翻译，节点 ID、类型、时间范围及 T/P/C linkage 永远取 canonical；在线脉络 UI 全量本地化，MeetingPack 同步携带导出前已生成的脉络语言版本。
- `ab16750` 新增“中文 / EN”阅读语言开关：在线界面与会议纪要同步切换；纪要译文采用不覆盖原文、revision-bound、保留 evidence marker 的分块 sidecar。MeetingPack 升级 v5，可离线携带并切换导出前已生成的双语纪要。服务中断后的 `visual_enrichment` 可在确认无活动 writer 时复用 transcript、slides 与 VL cache 阶段级续跑，不再要求重新上传或从头理解全部页面。
- `90d99c4` 正式待办与模型 action claim 分层：只有整场待办章节、非 informational 且有逐字稿依据的 action 进入 Web、MeetingPack、章节行动组和 RAG 行动加权；逐页误标保留为证据但不再污染待办。结论审计默认显示重点结论，并可切换查看全部证据。

## 2026-08-12

- `bc64f01` 在线工作台和 MeetingPack 的当前屏幕增加沉浸式放大、125%–300% 缩放、双击放大及前后逻辑页导航；切页更新播放时间和共享 Focus，但不自动开始播放。新增硬件适配层和无 GPU 回归：PyTorch 自动区分 CUDA/ROCm/CPU，旧 NVIDIA 自动从 BF16 回退 FP16；ASR、aligner、pyannote、文本/VL 模型路径及端口改为环境变量。README 从“钉钉闪记本地版”重写为本地多模态会议知识工作台，补充产品核心、架构、目录、数据资产和文档地图；同时新增跨机器部署/NVIDIA 验收矩阵、2 小时实测成本模型、无凭据部署模板和仓库级 `AGENTS.md`。
- `ba6ec1c` 重构在线工作台与 MeetingPack 的首屏阅读路径：通过质量门槛的 3–8 个一级论点“会议脉络”成为第一眼，纪要继续作为正式记录；脉络首屏只展开一级论点，选中后才展开当前分支。节点点击只建立语义 Focus，不改变播放时间；时间轴、逐字稿时间码和显式范围按钮才跳转播放，并持续联动高亮逐字稿、结论、时间范围和当前屏幕。无视频导出包使用冻结截图作为随音频/时间变化的内容舞台；Topic Map 缺失或不合格时安全回退会议纪要。
- `7e41911` `1bd8f51` 修复真实长会议的待办与议题阅读失真：常规纪要把带逐字稿 T ID 的结构化行动项作为“可核验待办”，原模型表中尚未关联来源的内容保留为默认收起的“待核实候选”，既不丢信息也不把建议伪装成正式任务；重复且易失控的“议题板块”Markdown 从常规纪要移到独立“会议脉络”。长会总体归纳设置结论/行动/议题数量上限并减少串行分块，Topic Map 的局部和全局 JSON 都支持只修语法的自动恢复，默认处理窗由约 8 分钟调整为 15 分钟。
- `ae285e9` 修复超长录屏会议在 VL 完成后仍生成终稿失败：总体摘要改为带 T/P 证据编号的连续分段归纳再全局合并；按页纪要继续使用原有受控分组。Web 重生成复用逐字稿、页面和有效 VL 缓存，只补缺失页面，并在成功后发布 ready 状态与刷新会议脉络。
- `63e4278` `5f88ca5` 修复屏幕内容价值误判：缺失或 reasoning 清洗后为空的 VL 说明现在显示“待解析”，不再因字数少自动降为“低信息”；只有明确的空白、过渡或会议 UI 信号才能判低。详细 VL 正文为空时降级为紧凑 JSON 视觉读取（标题/类型/摘要）并转成页面说明，仍失败才保持待解析，空正文永不进入成功缓存。
- `b2945ad` 将本机文本模型协议、上下文预算和语音草稿策略拆入独立 `meeting_core` 模块；短会议保持直接生成，超出上下文预算的长会议按稳定 T ID 连续分段，先提取事实笔记再全局合并，避免 HTTP 400 使渐进草稿缺失。在架构中预留“企业同事上传、本机队列处理、受控网页阅读与本机 LLM 对话”的身份、ACL、审计和存储边界；当前不开放网络监听。建立本文件作为人类可读索引；每一项的精确差异和验证记录继续由 Git 提交与工程走查承载。

- `a7bb83a` 增加可插队的串行优先级作业队列。
- `561a812` 保证语音草稿关闭 thinking，并明确区分空正文失败。
- `6dd94b1` 简化离线 MeetingPack 查看器。
- `fb836d7` 增加“语音草稿 → 视觉补充 → 多模态终稿”的渐进处理流程。
- `4980565` 重塑会议脉络、屏幕内容和常规纪要的阅读关系。
- `8d579d4` 优化章节结构与结论审计交互。
- `ef570b2` 增加章节和屏幕内容视图。
- `ae719b6` 优化多语言逐字稿和待办事项展示。
- `6f886ba` 修正终稿生成阶段标签。
- `eff9135` 修正窄侧栏中的作业进度可读性。
- `88a2eb2` 保证会议作业使用项目虚拟环境。
- `1abc18e` 增加产品介绍页面。
- `d9fb6fb` 优化会议回顾工作台 UX。

## 2026-08-11

- `101aec2` 记录 UX 路线与同类会议应用参考。
- `2a0b5e9` 发布混合检索、reranker 和 MeetingPack v2。
- `b102451` 增加带证据引用的单会议 RAG。
- `9a29889` 记录 Web 重启连接排空问题。
- `cd7cbd4` 记录渐进翻译 UX。
- `8e829ec` 增加逐批流式展现的逐字稿翻译。
- `7cc10b2` 记录逐字稿翻译试运行。
- `2b46c70` 增加会议上下文感知翻译。
- `abb0a41` 记录明确的质量验收入口。
- `e46c185` 明确会议结论质量验收入口。
- `bfcd603` 修复空纪要的质量状态。
- `1a82ea2` 记录纪要质量验收发布结果。
- `e15f3ba` 增加基于 evidence 的结论质量审计。
- `9cbc3d1` 将工程走查改为持续维护台账。
- `7a9829f` 修复导入会议媒体的持久回放。

## 2026-08-10

- `829fa8d` 增加证据关联 MeetingPack 和 RAG 导出。
- `cb6e6f6` 清洗组织架构模型输出。
- `ab253d3` 修复组织架构编辑后分支消失。
- `3895258` 修复删除节点时误删全部子节点。
- `4f97217` 增加组织节点就地快捷操作。
- `ceed460` 移除拖动组织节点时的确认弹窗。
- `d8e410d` 改善大型组织图缩放和布局。
- `f5d404d` 在声纹库 schema 迁移前自动备份。
- `354bf0a` 改善国际化人员身份与组织架构确认流程。
- `380e10c` 重构会议回顾工作台布局。
- `639a507` 增加本地会议助手与工程安全边界。
- `c4c10fd` 建立隔离私有数据的本地 Git 基线。
